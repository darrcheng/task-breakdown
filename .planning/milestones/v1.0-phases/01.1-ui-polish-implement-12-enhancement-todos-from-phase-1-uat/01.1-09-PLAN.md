---
phase: 01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/list/TaskListItem.tsx
  - src/components/task/CategoryCombobox.tsx
  - src/components/list/DayGroup.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Done tasks turn green (text + strikethrough decoration) during 1.5s departure animation"
    - "Category combobox shows X clear button when category is selected and dropdown is closed"
    - "Add button plus mark is optically centered using SVG icon instead of text glyph"
  artifacts:
    - path: "src/components/list/TaskListItem.tsx"
      provides: "Green departure animation with text-green-600 and decoration-green-600"
      contains: "text-green-600"
    - path: "src/components/task/CategoryCombobox.tsx"
      provides: "Clear button (X icon) for resetting selected category"
      contains: "handleClear"
    - path: "src/components/list/DayGroup.tsx"
      provides: "SVG Plus icon replacing text + glyph"
      contains: "Plus"
  key_links:
    - from: "src/components/list/TaskListItem.tsx"
      to: "departing state"
      via: "conditional green classes when departing=true"
      pattern: "departing.*text-green-600"
    - from: "src/components/task/CategoryCombobox.tsx"
      to: "onChange(0)"
      via: "handleClear callback resets category selection"
      pattern: "handleClear.*onChange\\(0\\)"
---

<objective>
Fix three cosmetic/minor UI gaps from UAT recheck: green departure animation for done tasks, clear button on category combobox, and SVG plus icon for add button.

Purpose: Close UAT recheck gaps 1, 2, and 4 (departing green text, category clear button, plus icon centering).
Output: Three source files modified with targeted fixes.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-06-SUMMARY.md
@.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-UAT-recheck.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green departure animation for done tasks</name>
  <files>src/components/list/TaskListItem.tsx</files>
  <action>
In TaskListItem.tsx, make the departing state turn the entire row green in addition to the existing strikethrough + fade:

1. On line 57 (the departing classes), add `text-green-600 decoration-green-600` alongside the existing `line-through opacity-0 transition-all duration-[1500ms]`.

2. Override child element colors when departing. The icon (line 75), title span (line 77), and status badge (lines 81-86) all have explicit `colors.text` classes that will override the parent's `text-green-600`. For each of these elements, add a conditional class: when `departing` is true, use `text-green-600` instead of `colors.text`.

Specifically:
- Line 75 icon: change `colors.text` to `departing ? 'text-green-600' : colors.text`
- Line 77 title span: change `colors.text` to `departing ? 'text-green-600' : colors.text`
- Lines 81-86 status badge: change `colors.text` to `departing ? 'text-green-600' : colors.text`

Do NOT change the status circle button (lines 61-72) — it already shows emerald-500 when done.
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. Visually inspect: when a task is marked done in list view, the entire row (icon, title, badge) should turn green text while fading out over 1.5 seconds.</verify>
  <done>Done tasks show green text (icon, title, status badge) with green strikethrough decoration during the 1.5-second departure animation.</done>
</task>

<task type="auto">
  <name>Task 2: Add clear button to category combobox</name>
  <files>src/components/task/CategoryCombobox.tsx</files>
  <action>
In CategoryCombobox.tsx, add a clear (X) button that appears when a category is selected and the dropdown is closed:

1. Import `X` from `lucide-react` (add to existing `Plus` import on line 2).

2. Create a `handleClear` callback (near the other handlers around line 74-80):
   ```
   const handleClear = useCallback((e: React.MouseEvent) => {
     e.stopPropagation();
     onChange(0);
     setSearch('');
     setIsOpen(false);
     setHighlightIndex(-1);
   }, [onChange]);
   ```

3. After the icon IIFE block (after line 154, before the input), render the X button conditionally:
   ```
   {!isOpen && selectedCategory && (
     <button
       type="button"
       onClick={handleClear}
       className="absolute right-2 top-2.5 p-0.5 rounded hover:bg-slate-200 text-slate-400 hover:text-slate-600 transition-colors z-10"
       aria-label="Clear category"
     >
       <X className="w-3.5 h-3.5" />
     </button>
   )}
   ```

4. Update the input className to add right padding when the clear button is visible. Change the padding logic:
   - Currently: `!isOpen && selectedCategory ? "pl-8 pr-3" : "px-3"`
   - Change to: `!isOpen && selectedCategory ? "pl-8 pr-8" : "px-3"`
   This gives space for both the icon on the left and the X button on the right.
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. In the task modal, select a category, close dropdown. An X button should appear on the right side of the combobox. Clicking X clears the selection back to "Select category..." placeholder.</verify>
  <done>Category combobox shows an X clear button when a category is selected and dropdown is closed. Clicking it resets to empty state with placeholder text.</done>
</task>

<task type="auto">
  <name>Task 3: Replace text plus glyph with SVG Plus icon in add button</name>
  <files>src/components/list/DayGroup.tsx</files>
  <action>
In DayGroup.tsx, replace the text `+` character with the lucide-react `Plus` SVG icon to fix optical centering:

1. Add `Plus` to the imports from `lucide-react`. There is no existing lucide import in this file, so add:
   `import { Plus } from 'lucide-react';`

2. On line 45-51, the add button currently renders a text `+` character. Replace:
   - Remove the `+` text content from the button (line 50)
   - Replace with `<Plus className="w-4 h-4" />`

3. Clean up classes that were only needed for the text glyph. On line 47, the button className has `text-lg font-medium leading-none` — remove these three classes since they are irrelevant for an SVG icon. Keep all other classes (`flex items-center justify-center w-7 h-7 rounded-full bg-slate-100 hover:bg-blue-100 hover:text-blue-600 text-slate-500 transition-colors`).
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. In list view, the + icon inside the circular add-task button on each day header should be perfectly centered (no downward offset).</verify>
  <done>Add button uses SVG Plus icon from lucide-react, perfectly centered in the circle with no font-metric offset.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- All three source files contain the expected changes
- No regressions in other TaskListItem, CategoryCombobox, or DayGroup functionality
</verification>

<success_criteria>
- Done tasks turn green (text + decoration) during departure animation
- Category combobox has a working clear (X) button
- Add button plus icon is perfectly centered (SVG, not text glyph)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-09-SUMMARY.md`
</output>
