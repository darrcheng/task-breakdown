---
phase: 01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/list/TaskListItem.tsx
  - src/components/list/DayGroup.tsx
  - src/components/task/TaskInlineCreate.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Done tasks show a strikethrough+fade departure animation before disappearing from the list"
    - "No pulsing amber ring or two-click reopen logic exists"
    - "The + mark in the add-task circle button is optically centered"
    - "Inline create category field is proportionally larger, task title field proportionally smaller"
  artifacts:
    - path: "src/components/list/TaskListItem.tsx"
      provides: "Done task departure animation with delay before DB write"
      contains: "setTimeout"
    - path: "src/components/list/DayGroup.tsx"
      provides: "Optically centered + glyph in add button"
      contains: "leading-none"
    - path: "src/components/task/TaskInlineCreate.tsx"
      provides: "Balanced flex proportions for title and category fields"
      contains: "flex-[3]"
  key_links:
    - from: "src/components/list/TaskListItem.tsx"
      to: "src/db/database.ts"
      via: "delayed db.tasks.update after animation"
      pattern: "setTimeout.*db\\.tasks\\.update"
---

<objective>
Fix three list-view UAT gaps: (1) done tasks disappear too fast with unwanted amber ring, (2) add button + not centered, (3) inline create field proportions wrong.

Purpose: Close UAT test 3, test 8, and test 10 gaps from Phase 01.1 user testing.
Output: Three corrected list-view components with proper animations, centering, and proportions.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-UAT.md
@.planning/debug/done-tasks-disappear-immediately.md
@.planning/debug/inline-create-field-sizing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Done task departure animation and remove amber ring</name>
  <files>src/components/list/TaskListItem.tsx</files>
  <action>
In `src/components/list/TaskListItem.tsx`:

1. **Remove the confirmReopen state and two-click logic entirely:**
   - Delete `const [confirmReopen, setConfirmReopen] = useState(false);` (line 14)
   - Remove the `useState` import if no other state remains (but keep it since other state might be added)
   - In `handleStatusClick`: remove the entire `if (task.status === 'done' && !confirmReopen)` block (lines 31-35) and the `setConfirmReopen(false)` call (line 36)
   - In the status button's className: remove the `confirmReopen` ternary branch (line 59-60) — the `'bg-emerald-500 border-emerald-500 ring-2 ring-offset-1 ring-amber-400 animate-pulse'` line
   - In the title attribute: remove the `confirmReopen` ternary (lines 68-69)

2. **Add departure animation when task is marked done:**
   - Add a `departing` state: `const [departing, setDeparting] = useState(false);`
   - In `handleStatusClick`, when the next status would be `'done'` (i.e., current status is `'in-progress'` and `getNextStatus` returns `'done'`):
     - Instead of writing to DB immediately, set `departing = true`
     - After 1500ms timeout, THEN write the done status to DB via `db.tasks.update`
   - For non-done transitions (todo->in-progress, done->todo), write to DB immediately as before
   - On the outer div, add conditional classes when `departing` is true: `'line-through opacity-0 transition-all duration-[1500ms]'`
   - Import `getNextStatus` is already imported — use it to check: `const nextStatus = getNextStatus(task.status); if (nextStatus === 'done') { setDeparting(true); setTimeout(...) } else { db.tasks.update(...) }`

The result: when a task transitions to done, it visually fades out with strikethrough over 1.5 seconds, then gets written to DB (which removes it from the live query). No amber ring. No two-click confirm.
  </action>
  <verify>Run `npm run build` to confirm no TypeScript errors. Visually: mark an in-progress task as done and confirm it fades out over ~1.5s with strikethrough before disappearing. Confirm no amber pulsing ring anywhere.</verify>
  <done>Done tasks show strikethrough + fade animation for 1.5s before disappearing. No confirmReopen state, no pulsing amber ring. Non-done transitions (todo->in-progress) still happen instantly.</done>
</task>

<task type="auto">
  <name>Task 2: Center add button plus mark and fix inline create field proportions</name>
  <files>src/components/list/DayGroup.tsx, src/components/task/TaskInlineCreate.tsx</files>
  <action>
**DayGroup.tsx (add button centering):**
In `src/components/list/DayGroup.tsx`, the button on lines 45-51:
```
<button ... className="flex items-center justify-center w-7 h-7 rounded-full bg-slate-100 hover:bg-blue-100 hover:text-blue-600 text-slate-500 transition-colors text-lg font-medium">
  +
</button>
```
Add `leading-none` to the className string. This removes the line-height that prevents flexbox from optically centering the `+` glyph. The final className should be:
`"flex items-center justify-center w-7 h-7 rounded-full bg-slate-100 hover:bg-blue-100 hover:text-blue-600 text-slate-500 transition-colors text-lg font-medium leading-none"`

**TaskInlineCreate.tsx (field proportions):**
In `src/components/task/TaskInlineCreate.tsx`:
- Line 64: Change the title input's `flex-1` to `flex-[3]` — this makes it take 3/4 of available space instead of all of it
- Line 69: On the category select, remove `max-w-[140px]` and add `flex-[1] min-w-[160px]` — this gives the category field a proportional 1/4 share with a minimum width

The select className should become:
`"px-2 py-2 border border-blue-300 rounded-md text-sm focus:ring-2 focus:ring-blue-400 focus:border-blue-400 outline-none bg-blue-50/50 flex-[1] min-w-[160px]"`
  </action>
  <verify>Run `npm run build` to confirm no errors. Visually check: (1) the + glyph is centered in the circle button in list view day headers, (2) the inline create form shows a wider category selector and slightly narrower task title field with balanced proportions.</verify>
  <done>Plus mark is optically centered in the circle button. Inline create category field is wider (~25% of row) with min-w-[160px], title field uses flex-[3] (~75% of row). Both visually balanced.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- In list view: mark an in-progress task as done — it fades out with strikethrough over ~1.5 seconds
- In list view: no amber pulsing ring appears on any task
- In list view: the + button in day headers has a visually centered plus mark
- In list view: the inline create form has balanced field proportions
</verification>

<success_criteria>
UAT test 3 (done task departure) closes: tasks fade out with delay before disappearing, no amber ring.
UAT test 8 (add button centering) closes: + mark is optically centered.
UAT test 10 (inline field sizing) closes: category field larger, title field proportionally smaller.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-06-SUMMARY.md`
</output>
