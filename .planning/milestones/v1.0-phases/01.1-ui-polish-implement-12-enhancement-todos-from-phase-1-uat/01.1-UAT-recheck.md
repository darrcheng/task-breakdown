---
status: resolved
phase: 01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat
source: 01.1-06-SUMMARY.md, 01.1-07-SUMMARY.md, 01.1-08-SUMMARY.md
started: 2026-02-22T22:00:00Z
updated: 2026-02-22T23:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Done Task Departure Animation
expected: In list view, mark a task as done. It should show a strikethrough + fade-out animation over ~1.5 seconds, then disappear. No pulsing amber ring, no two-click confirmation.
result: issue
reported: "It shows a strikethrough, but it should turn green when it strikes through"
severity: cosmetic

### 2. Category Combobox Empty Default
expected: Open a new task modal. The category field should start empty with "Select category..." placeholder (not auto-selecting the first category).
result: issue
reported: "yea, but can I get a clear to clear the category?"
severity: minor

### 3. Category Combobox Selected Icon
expected: In the task modal, select a category from the combobox. When the dropdown closes, the selected category's icon should appear to the left of the category name in the input field.
result: pass

### 4. Add Button Plus Centering
expected: In list view, the + mark inside the circular add-task button on each day header is visually centered (not offset downward).
result: issue
reported: "still ofset downwards"
severity: major

### 5. Inline Create Field Proportions
expected: In list view, click to add a task inline. The category selector should be noticeably wider than before (~25% of the row), and the title input slightly narrower (~75%).
result: pass

### 6. Full Calendar Cell Click
expected: In calendar view, click on any blank area of a day cell (not the date number, not a task). The task creation modal should open for that date.
result: pass

### 7. Keyboard Shortcuts Remapped
expected: With no input focused and no modal open: press c (switches to calendar view), l (switches to list view), n (opens new task modal defaulting to today). Old m and w shortcuts should no longer work.
result: issue
reported: "Why don't the m and w don't work anymore? Pressing the n to open a new task modal inserts an n at the start of the task name"
severity: major

### 8. Date Field in Task Modal
expected: Open the task creation modal (via clicking a cell or the add button). A date field with a native calendar dropdown should be visible between the title and status fields. Changing the date and saving should persist the new date.
result: issue
reported: "Why's it so ugly? Can we have a notion-style calendar dropdown?"
severity: major

## Summary

total: 8
passed: 3
issues: 5
pending: 0
skipped: 0

## Gaps

- truth: "Done task strikethrough animation should turn green during departure"
  status: resolved
  reason: "User reported: It shows a strikethrough, but it should turn green when it strikes through"
  severity: cosmetic
  test: 1
  root_cause: "Departing state applies line-through + opacity-0 but no green text color. Child elements have explicit colors.text classes that override any inherited color."
  artifacts:
    - path: "src/components/list/TaskListItem.tsx"
      issue: "Line 57: departing classes missing text-green-600 and decoration-green-600. Lines 75,77,81-86: child elements have explicit colors.text overriding parent"
  missing:
    - "Add text-green-600 and decoration-green-600 to departing classes on line 57"
    - "Override colors.text with text-green-600 on icon (line 75), title span (line 77), and status badge when departing"
  debug_session: ".planning/debug/departure-green-text.md"

- truth: "Category combobox should have a clear/reset button to deselect category"
  status: resolved
  reason: "User reported: yea, but can I get a clear to clear the category?"
  severity: minor
  test: 2
  root_cause: "CategoryCombobox has no UI element to clear selection. onChange(0) resets to no-category state but nothing triggers it."
  artifacts:
    - path: "src/components/task/CategoryCombobox.tsx"
      issue: "No clear button exists. When category is selected and dropdown closed, right side of input is empty."
  missing:
    - "Import X from lucide-react"
    - "Add handleClear callback that calls onChange(0), resets search, closes dropdown"
    - "Render X button when !isOpen && selectedCategory, positioned absolute right-2"
    - "Change input right padding from pr-3 to pr-8 when category selected"
  debug_session: ".planning/debug/category-combobox-clear-button.md"

- truth: "Add button plus mark is optically centered in circle"
  status: resolved
  reason: "User reported: still ofset downwards"
  severity: major
  test: 4
  root_cause: "The + is a text glyph with asymmetric font metrics. CSS centering centers the line box, not the visible ink. No CSS fix can resolve this — must use SVG icon instead."
  artifacts:
    - path: "src/components/list/DayGroup.tsx"
      issue: "Lines 45-51: Button uses text + character instead of SVG icon. leading-none fix insufficient due to font metric asymmetry."
  missing:
    - "Import Plus from lucide-react (already used in CategoryCombobox and CategoryManager)"
    - "Replace text + with <Plus className='w-4 h-4' /> in the button"
    - "Remove text-lg font-medium leading-none classes (only needed for text glyph)"
  debug_session: ".planning/debug/add-task-plus-glyph-offset.md"

- truth: "Keyboard shortcuts: m/w should still work for month/week views. n shortcut should not insert character into task name field"
  status: resolved
  reason: "User reported: Why don't the m and w don't work anymore? Pressing the n to open a new task modal inserts an n at the start of the task name"
  severity: major
  test: 7
  root_cause: "Bug 1: Missing e.preventDefault() in keyboard handler — 'n' keydown opens modal but browser default inserts char into auto-focused title input. Bug 2: Commit 3b97d79 deleted m/w cases entirely instead of keeping them."
  artifacts:
    - path: "src/App.tsx"
      issue: "Lines 53-79: No e.preventDefault() on any switch case. m and w cases were deleted instead of kept."
    - path: "src/components/ui/SettingsModal.tsx"
      issue: "SHORTCUTS array missing m and w entries"
  missing:
    - "Add e.preventDefault() to all matched switch cases in keyboard handler"
    - "Re-add case 'm' (month view) and case 'w' (week view) to switch statement"
    - "Add m and w entries to SHORTCUTS array in SettingsModal"
  debug_session: ".planning/debug/n-key-inserts-char-and-missing-mw-shortcuts.md"

- truth: "Date field should be a Notion-style calendar dropdown, not ugly native input"
  status: resolved
  reason: "User reported: Why's it so ugly? Can we have a notion-style calendar dropdown?"
  severity: major
  test: 8
  root_cause: "Using native <input type='date'> which renders browser's unstyled date picker. Project has date-fns + getCalendarDays() + lucide-react already — everything needed for a custom DatePicker component."
  artifacts:
    - path: "src/components/task/TaskForm.tsx"
      issue: "Lines 93-109: Uses native <input type='date'> which cannot be styled"
    - path: "src/utils/dates.ts"
      issue: "Already has getCalendarDays(), formatDateKey(), isToday(), isSameMonth() — all reusable for custom picker"
  missing:
    - "Create src/components/task/DatePicker.tsx — custom dropdown with Calendar icon trigger, month nav, day grid using getCalendarDays()"
    - "Replace <input type='date'> in TaskForm with <DatePicker value={date} onChange={setDate} />"
  debug_session: ""
