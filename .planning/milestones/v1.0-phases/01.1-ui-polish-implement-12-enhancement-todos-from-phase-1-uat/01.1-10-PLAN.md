---
phase: 01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/ui/SettingsModal.tsx
  - src/components/task/DatePicker.tsx
  - src/components/task/TaskForm.tsx
  - src/components/task/TaskModal.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Pressing n with no input focused opens new task modal without inserting n into the title field"
    - "Pressing m switches to month calendar view, w switches to week calendar view"
    - "Settings modal shows m and w shortcut entries alongside existing shortcuts"
    - "Date field in task modal is a custom Notion-style calendar dropdown, not a native browser date input"
    - "Selecting a date in the custom calendar dropdown persists the date when task is saved"
  artifacts:
    - path: "src/App.tsx"
      provides: "preventDefault on all shortcut cases, m/w cases restored"
      contains: "e.preventDefault"
    - path: "src/components/ui/SettingsModal.tsx"
      provides: "m and w entries in SHORTCUTS array"
      contains: "Month view"
    - path: "src/components/task/DatePicker.tsx"
      provides: "Custom Notion-style calendar dropdown component"
      contains: "getCalendarDays"
    - path: "src/components/task/TaskForm.tsx"
      provides: "DatePicker integration replacing native input type=date"
      contains: "DatePicker"
  key_links:
    - from: "src/App.tsx"
      to: "keyboard handler"
      via: "e.preventDefault() in each matched switch case"
      pattern: "e\\.preventDefault"
    - from: "src/components/task/DatePicker.tsx"
      to: "src/utils/dates.ts"
      via: "getCalendarDays for day grid rendering"
      pattern: "getCalendarDays"
    - from: "src/components/task/TaskForm.tsx"
      to: "src/components/task/DatePicker.tsx"
      via: "DatePicker component replaces native input"
      pattern: "DatePicker.*value.*onChange"
---

<objective>
Fix keyboard shortcuts (missing preventDefault, restore m/w bindings) and replace the native date input with a custom Notion-style calendar dropdown.

Purpose: Close UAT recheck gaps 7 (keyboard shortcuts) and 8 (ugly date picker).
Output: Keyboard shortcuts work correctly without character insertion; date picker is a polished custom calendar dropdown.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-08-SUMMARY.md
@.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-UAT-recheck.md
@src/utils/dates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix keyboard shortcuts — add preventDefault and restore m/w</name>
  <files>src/App.tsx, src/components/ui/SettingsModal.tsx</files>
  <action>
**In src/App.tsx**, fix the keyboard handler (lines 53-79):

1. Add `e.preventDefault()` as the FIRST line inside every matched `case` block. This prevents the browser default action (e.g., the 'n' character being inserted into the auto-focused title input when the modal opens). Add it to cases: 'j', 'k', 't', 'c', 'l', 'n', '?'.

2. Re-add the `m` and `w` cases that were deleted in commit 3b97d79. Add them BEFORE the existing 'c' case:
   ```
   case 'm': // Month view
     e.preventDefault();
     setCalendarView('month');
     setViewMode('calendar');
     break;
   case 'w': // Week view
     e.preventDefault();
     setCalendarView('week');
     setViewMode('calendar');
     break;
   ```
   Note: m and w should also switch to calendar viewMode (since month/week are calendar sub-views). If user is in list view and presses 'm', they should see the month calendar.

**In src/components/ui/SettingsModal.tsx**, add m and w to the SHORTCUTS array (lines 13-21):

Add these two entries after the 't' entry and before the 'c' entry:
```
{ key: 'm', description: 'Month view' },
{ key: 'w', description: 'Week view' },
```
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. Test mentally: pressing 'n' with no input focused should call `e.preventDefault()` before `setModalState(...)`, so no character gets typed into the auto-focused title field. Pressing 'm' shows month calendar, 'w' shows week calendar.</verify>
  <done>All keyboard shortcuts call preventDefault. m=month view, w=week view restored. Settings modal lists all shortcuts including m and w.</done>
</task>

<task type="auto">
  <name>Task 2: Create custom Notion-style DatePicker component</name>
  <files>src/components/task/DatePicker.tsx</files>
  <action>
Create a new file `src/components/task/DatePicker.tsx` — a custom Notion-style calendar dropdown that replaces the native `<input type="date">`.

**Props interface:**
```typescript
interface DatePickerProps {
  value: string;       // 'yyyy-MM-dd' format
  onChange: (date: string) => void;
  required?: boolean;
}
```

**Component structure:**

1. **State:** `isOpen` (boolean for dropdown), `viewMonth` (Date for the currently displayed month in the dropdown, initialized from `value` or today).

2. **Trigger button** (not an input — a styled button that looks like a form field):
   - Shows the formatted date (e.g., "Feb 22, 2026") using `format(parseISO(value), 'MMM d, yyyy')` from date-fns.
   - Shows a Calendar icon from lucide-react on the left side.
   - If no value, shows "Select date..." placeholder in slate-400.
   - Clicking toggles `isOpen`.
   - Styled to match other form fields: `w-full px-3 py-2 border border-slate-300 rounded-md text-sm text-left` with focus ring styles.

3. **Dropdown panel** (when `isOpen`):
   - Absolutely positioned below the trigger, z-50, white bg, rounded-lg, shadow-lg, border.
   - Width: `w-72` (288px — standard calendar width).

4. **Month navigation header** inside dropdown:
   - Left arrow (`ChevronLeft` from lucide-react) and right arrow (`ChevronRight`) buttons.
   - Center: month and year label, e.g., "February 2026" using `format(viewMonth, 'MMMM yyyy')`.
   - Clicking arrows calls `setViewMonth(prev => addMonths(prev, 1))` or `subMonths`.

5. **Day-of-week headers:**
   - Row of 7 abbreviated day names: Su, Mo, Tu, We, Th, Fr, Sa (or Mo-first if weekStartsOn=1, but for simplicity hardcode Su-Sa since the app default is Sunday).
   - Styled: `text-xs font-medium text-slate-400 text-center`.

6. **Day grid:**
   - Use `getCalendarDays(viewMonth)` from `../../utils/dates` to get the 35 or 42 days.
   - Render as a CSS grid: `grid grid-cols-7`.
   - Each day is a button with:
     - `text-sm` size, `w-8 h-8` (or `w-9 h-9`), `rounded-full` for hover/selection.
     - **Today highlight:** If `isToday(day)` from dates.ts, show a subtle dot or ring: `ring-1 ring-blue-400 font-semibold`.
     - **Selected highlight:** If this day matches `value`, show `bg-blue-600 text-white font-semibold`.
     - **Other month days:** If `!isSameMonth(day, viewMonth)` from dates.ts, show `text-slate-300`.
     - **Current month days:** `text-slate-700 hover:bg-slate-100`.
   - Clicking a day calls `onChange(formatDateKey(day))` from dates.ts and sets `isOpen` to false.

7. **Click-outside handling:**
   - Use a ref on the container div.
   - Add a `useEffect` with a mousedown listener that checks if click is outside the ref, and if so, closes the dropdown.
   - Clean up the listener on unmount.

**Imports needed:**
- `useState, useRef, useEffect` from react
- `format, parseISO, addMonths, subMonths` from date-fns
- `Calendar, ChevronLeft, ChevronRight` from lucide-react
- `getCalendarDays, formatDateKey, isToday, isSameMonth` from ../../utils/dates

**Style approach:** Match the existing app aesthetic — white backgrounds, slate text, blue-600 accents, rounded-md borders, subtle shadows. Keep it clean and minimal like Notion's date picker.
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. The file should export a `DatePicker` component with the described props.</verify>
  <done>DatePicker.tsx exists with a fully styled Notion-like calendar dropdown, using existing date utilities from the project.</done>
</task>

<task type="auto">
  <name>Task 3: Replace native date input with DatePicker in TaskForm</name>
  <files>src/components/task/TaskForm.tsx</files>
  <action>
In TaskForm.tsx, replace the native `<input type="date">` with the custom DatePicker:

1. Add import at top: `import { DatePicker } from './DatePicker';`

2. Remove the native date input block (lines 93-109). Replace the entire `{/* Date */}` section with:
   ```jsx
   {/* Date */}
   <div>
     <label className="block text-sm font-medium text-slate-700 mb-1">
       Date
     </label>
     <DatePicker value={date} onChange={setDate} required />
   </div>
   ```

3. The `date` state, `setDate`, and the `onSubmit` data flow already work correctly (they use 'yyyy-MM-dd' string format). No changes needed to state management or the submit handler.

4. Increase `MODAL_HEIGHT` in TaskModal.tsx from 560 to 620 to accommodate the calendar dropdown when open. Read TaskModal.tsx first, then update the MODAL_HEIGHT constant on line 60.
  </action>
  <verify>Run `npm run build` — zero TypeScript errors. Open the task modal — the date field should show a styled button with a calendar icon. Clicking it opens a month-grid dropdown. Selecting a date updates the field and closes the dropdown.</verify>
  <done>TaskForm uses custom DatePicker component instead of native date input. Calendar dropdown opens on click, selecting a date persists correctly on save.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- All keyboard shortcuts work with preventDefault (no character insertion)
- m=month, w=week shortcuts work and appear in settings
- DatePicker renders a Notion-style calendar dropdown
- Date selection persists when task is saved
</verification>

<success_criteria>
- Pressing n opens new task modal without inserting 'n' into the title
- Pressing m/w switches to month/week calendar views
- Settings modal shows all keyboard shortcuts including m and w
- Date field is a polished custom calendar dropdown (not native browser picker)
- Date changes persist correctly through create and edit flows
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat/01.1-10-SUMMARY.md`
</output>
