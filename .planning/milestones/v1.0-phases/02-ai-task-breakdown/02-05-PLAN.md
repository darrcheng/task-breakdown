---
phase: 02-ai-task-breakdown
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - src/hooks/useBreakdown.ts
  - src/components/task/SubtaskReview.tsx
  - src/components/task/BreakdownButton.tsx
  - src/components/task/SubtaskList.tsx
  - src/components/task/TaskModal.tsx
autonomous: false
requirements: [AI-05, AI-06]

must_haves:
  truths:
    - "User can regenerate subtasks with unpinned ones replaced and pinned ones preserved"
    - "User can recursively break down subtasks up to 3 levels deep"
    - "Break it down button is hidden for tasks at depth 3"
    - "Recursive breakdown uses the same full review flow at every depth level"
    - "Regeneration replaces only unpinned subtasks while keeping pinned ones in place"
    - "All Phase 2 success criteria are met end-to-end"
  artifacts:
    - path: "src/hooks/useBreakdown.ts"
      provides: "Regeneration logic with pin/keep support"
      contains: "regenerateSubtasks"
    - path: "src/components/task/SubtaskReview.tsx"
      provides: "Regenerate button with pinned subtask preservation"
      contains: "Regenerate"
  key_links:
    - from: "src/hooks/useBreakdown.ts"
      to: "src/ai/provider-factory.ts"
      via: "regeneration calls createProvider again for new subtasks"
      pattern: "createProvider"
    - from: "src/components/task/BreakdownButton.tsx"
      to: "src/types/index.ts"
      via: "depth check against Task.depth"
      pattern: "depth.*3"
---

<objective>
Add selective regeneration (pin/keep toggle) and recursive breakdown (up to 3 levels deep), then verify the complete Phase 2 feature set end-to-end.

Purpose: This plan completes the AI task breakdown feature set. Regeneration with pinning lets users keep good suggestions while replacing weak ones. Recursive breakdown enables going deeper into complex subtasks. The verification checkpoint ensures everything works together.
Output: Regeneration with pin support, recursive breakdown, and verified end-to-end feature.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-task-breakdown/02-RESEARCH.md
@.planning/phases/02-ai-task-breakdown/02-CONTEXT.md
@.planning/phases/02-ai-task-breakdown/02-03-SUMMARY.md
@.planning/phases/02-ai-task-breakdown/02-04-SUMMARY.md
@src/hooks/useBreakdown.ts
@src/components/task/SubtaskReview.tsx
@src/components/task/BreakdownButton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selective regeneration with pin/keep support and recursive breakdown</name>
  <files>src/hooks/useBreakdown.ts, src/components/task/SubtaskReview.tsx, src/components/task/BreakdownButton.tsx, src/components/task/SubtaskList.tsx, src/components/task/TaskModal.tsx</files>
  <action>
**src/hooks/useBreakdown.ts — Add regeneration:**
Add `regenerateSubtasks(task: Task)` method to the hook:
1. Collect pinned subtasks from current state (items where `pinned === true` and `removed === false`)
2. Transition to 'generating' state, keeping pinned subtasks in the array
3. Call `provider.generateSubtasks()` with modified prompt: include pinned subtask titles in the context so AI generates DIFFERENT subtasks (not duplicates)
4. As new subtasks stream in, append after pinned ones
5. On complete, transition to 'reviewing' with merged array (pinned first, then new)
6. Count param: generate `targetCount - pinnedCount` new subtasks (e.g., if 2 pinned and target is 4, generate 2 new)

Add `togglePin(id: string)` method that toggles the `pinned` flag on a ReviewSubtask.

**src/components/task/SubtaskReview.tsx — Add Regenerate button:**
Add a "Regenerate" button to the action bar (between Accept All and Cancel):
- Lucide `RefreshCw` icon + "Regenerate" text
- Disabled during 'generating' state
- Tooltip/hint: "Pinned subtasks will be kept, unpinned will be replaced"
- Show pinned count if > 0: "Regenerate (keeping 2 pinned)"

**src/components/task/BreakdownButton.tsx — Depth guard:**
Ensure the button respects `task.depth`:
- Show button when `(task.depth ?? 0) < 3`
- Hide completely (return null) when depth >= 3
- Add depth indicator when depth > 0: small text "(Level {depth + 1})" next to button

**src/components/task/SubtaskList.tsx — Recursive breakdown entry:**
In the subtask list, each subtask row should show a small "Break down" icon button (Sparkles icon, 16px) when:
- The subtask's depth < 3
- Clicking it calls `onOpenSubtask(subtask)` to navigate to that subtask's modal where the BreakdownButton is available
This is NOT a direct breakdown trigger — it navigates to the subtask modal where the same full review flow is used (per user decision: "Recursive breakdown uses the same full review flow at every depth level").

**src/components/task/TaskModal.tsx — Recursive support:**
Ensure the modal correctly handles tasks at any depth (0, 1, 2, 3):
- BreakdownButton visibility tied to `task.depth < 3`
- SubtaskList shows for any task that has subtasks (regardless of depth)
- "Back to parent" link works for subtasks at any depth
- The accept flow in useBreakdown correctly sets `depth = parentTask.depth + 1` for new subtasks
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds. In dev mode with a configured API key:
1. Create a task, break it down -> verify 3-5 subtasks generated
2. Pin 2 subtasks, regenerate -> verify pinned ones stay, unpinned replaced
3. Accept subtasks -> verify they appear in calendar on same day
4. Open a subtask, break IT down -> verify depth-2 sub-subtasks generated
5. Open a depth-2 subtask -> verify "Break it down" button hidden at depth 3
  </verify>
  <done>
Regeneration preserves pinned subtasks and replaces unpinned ones. Recursive breakdown works up to 3 levels deep with depth guard. Same full review flow at every depth level.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 AI Task Breakdown feature set</name>
  <files>src/components/task/TaskModal.tsx</files>
  <action>
No code changes — human verification of complete Phase 2 feature set covering all 7 AI requirements:
- AI-01: "Break it down" button in task modal generates 3-5 subtasks
- AI-02: Edit AI-generated subtask titles inline in review preview
- AI-03: Drag-to-reorder subtasks in review preview
- AI-04: Remove individual subtasks via X button in review preview
- AI-05: Regenerate subtasks with pin/keep toggle preserving kept ones
- AI-06: Recursive breakdown up to 3 levels deep with same review flow
- AI-07: Provider selection (Claude/Gemini) in settings with encrypted key storage
  </action>
  <verify>
Run `npm run dev` and open http://localhost:5173

**1. Provider Setup (AI-07):**
- Open Settings (? key) -> verify AI Provider section exists
- Select a provider (Claude recommended) and enter API key
- Click "Test Connection" -> verify success message
- Close settings

**2. First Breakdown (AI-01):**
- Create a task (e.g., "Plan a birthday party")
- Click the task to open modal
- Click "Break it down" button
- Verify: subtasks stream in one-by-one (progressive rendering)
- Verify: 3-5 subtasks generated with relevant titles

**3. Review Controls (AI-02, AI-03, AI-04):**
- In the review preview:
  - Click a subtask title -> edit it inline -> press Enter to save
  - Drag a subtask to reorder -> verify new order persists
  - Click X on a subtask -> verify it's removed/hidden
- Click "Accept All"
- Verify: remaining subtasks appear in calendar on same day as parent

**4. Parent Display:**
- In calendar view: verify parent task shows subtask count badge
- Click parent task -> verify modal shows nested subtask list
- Check/complete all subtasks -> verify "All subtasks done — mark parent complete?" prompt

**5. Regeneration (AI-05):**
- Create another task and break it down
- Pin 2 subtasks (click pin icon)
- Click "Regenerate"
- Verify: pinned subtasks remain, unpinned are replaced with new ones

**6. Recursive Breakdown (AI-06):**
- Accept subtasks for a task
- Open a subtask in the modal
- Click "Break it down" on the subtask
- Verify: sub-subtasks generated (depth 2)
- Accept sub-subtasks
- Open a sub-subtask (depth 2)
- Break it down -> accept (depth 3)
- Open a depth-3 task -> verify "Break it down" button is HIDDEN

**7. Provider Switch (AI-07):**
- Open Settings -> change provider -> save new key
- Break down a new task -> verify it works with new provider
  </verify>
  <done>
All 7 AI requirements (AI-01 through AI-07) verified and working. Phase 2 success criteria met. User approved the complete feature set.
  </done>
</task>

</tasks>

<verification>
- All 7 AI requirements (AI-01 through AI-07) verified
- Regeneration preserves pinned subtasks correctly
- Recursive breakdown works at all depth levels (0->1->2->3) with depth-3 guard
- Provider can be switched without data loss
- API keys encrypted in storage
- Subtasks behave as first-class tasks in calendar/list views
</verification>

<success_criteria>
Complete Phase 2 feature set verified end-to-end: task breakdown with streaming, review with edit/reorder/remove, selective regeneration with pin/keep, recursive breakdown up to 3 levels, swappable AI providers with encrypted key storage. All success criteria from ROADMAP met.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-task-breakdown/02-05-SUMMARY.md`
</output>
