---
phase: 01-local-first-foundation
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - src/components/dnd/DndProvider.tsx
  - src/components/dnd/DraggableTask.tsx
  - src/components/dnd/DroppableDay.tsx
  - src/components/calendar/CalendarGrid.tsx
  - src/components/calendar/WeekView.tsx
  - src/components/calendar/DayCell.tsx
  - src/components/list/ListView.tsx
  - src/components/list/DayGroup.tsx
  - src/components/list/TaskListItem.tsx
  - src/App.tsx
autonomous: true
requirements:
  - TASK-03
must_haves:
  truths:
    - "User can drag a task card from one day to another in calendar view"
    - "User can drag a task from one day group to another in list view"
    - "Dragged task's date updates in the database"
    - "Both source and target day cells update reactively after drop"
    - "Visual feedback shows valid drop targets during drag"
  artifacts:
    - path: "src/components/dnd/DndProvider.tsx"
      provides: "DndContext wrapper with drag end handler"
      min_lines: 25
    - path: "src/components/dnd/DraggableTask.tsx"
      provides: "Wrapper making task cards draggable"
      min_lines: 15
    - path: "src/components/dnd/DroppableDay.tsx"
      provides: "Wrapper making day cells droppable zones"
      min_lines: 15
  key_links:
    - from: "src/components/dnd/DndProvider.tsx"
      to: "src/db/database.ts"
      via: "db.tasks.update on drag end"
      pattern: "db\\.tasks\\.update"
    - from: "src/components/dnd/DroppableDay.tsx"
      to: "src/components/calendar/DayCell.tsx"
      via: "wraps DayCell content as drop zone"
      pattern: "useDroppable"
    - from: "src/components/dnd/DraggableTask.tsx"
      to: "src/components/task/TaskCard.tsx"
      via: "wraps TaskCard as draggable"
      pattern: "useDraggable"
---

<objective>
Add drag-to-reschedule functionality using dnd-kit, enabling users to move tasks between days in both calendar and list views.

Purpose: TASK-03 requires drag-and-drop rescheduling. This is a key interaction for reorganizing tasks across days.
Output: DnD wrapper components, integrated into both calendar and list views.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-first-foundation/01-RESEARCH.md
@.planning/phases/01-local-first-foundation/01-CONTEXT.md
@.planning/phases/01-local-first-foundation/01-01-SUMMARY.md
@.planning/phases/01-local-first-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DnD wrapper components (DndProvider, DraggableTask, DroppableDay)</name>
  <files>
    src/components/dnd/DndProvider.tsx
    src/components/dnd/DraggableTask.tsx
    src/components/dnd/DroppableDay.tsx
  </files>
  <action>
    **src/components/dnd/DndProvider.tsx:**
    - Wraps children in `DndContext` from @dnd-kit/core
    - Configures sensors: PointerSensor with activation constraint (distance: 8px to prevent accidental drags on click)
    - `onDragEnd` handler:
      1. Extract `active.data.current.task` (the dragged task)
      2. Extract `over.id` (the droppable day's date string)
      3. If `over` exists and the date changed: `db.tasks.update(task.id, { date: newDate, updatedAt: new Date() })`
      4. useLiveQuery in both source and target DayCells auto-updates
    - `onDragStart` handler: Set active drag state for DragOverlay
    - Renders `DragOverlay` with a copy of the dragged TaskCard for smooth visual feedback
    - Props: `children: ReactNode`, `categoryMap: Map<number, Category>`
    - Export the active drag state type if needed by children

    **src/components/dnd/DroppableDay.tsx:**
    - Uses `useDroppable` from @dnd-kit/core with `id` set to the date string ('YYYY-MM-DD')
    - Visual feedback when `isOver`: subtle highlight (e.g., `bg-blue-50`, `ring-2 ring-blue-200`)
    - Wraps children (the day cell content)
    - Props: `dateStr: string`, `children: ReactNode`
    - The `setNodeRef` is applied to the wrapper div

    **src/components/dnd/DraggableTask.tsx:**
    - Uses `useDraggable` from @dnd-kit/core with `id` set to `task-${task.id}`
    - Stores `{ task }` in `data` prop for access in onDragEnd
    - Applies `transform` style from useDraggable for visual movement
    - While dragging: original element becomes semi-transparent (opacity-50)
    - Wraps children (the TaskCard or TaskListItem)
    - Props: `task: Task`, `children: ReactNode`
    - Applies drag handle attributes/listeners to the wrapper div
    - Uses `CSS.Transform.toString(transform)` from @dnd-kit/utilities for transform style

    IMPORTANT: Distinguish between click (for edit) and drag (for move). The PointerSensor distance constraint (8px) handles this — a short click won't trigger drag, only a sustained drag motion.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero type errors.
    All three components compile and export correctly.
  </verify>
  <done>
    DndProvider wraps the app with DndContext and handles drag-end logic to update task dates. DroppableDay makes day cells valid drop targets with visual feedback. DraggableTask makes task cards/items draggable with proper data passing. DragOverlay shows a preview of the dragged task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate drag-and-drop into calendar view and list view</name>
  <files>
    src/components/calendar/CalendarGrid.tsx
    src/components/calendar/WeekView.tsx
    src/components/calendar/DayCell.tsx
    src/components/list/ListView.tsx
    src/components/list/DayGroup.tsx
    src/components/list/TaskListItem.tsx
    src/App.tsx
  </files>
  <action>
    **src/App.tsx:**
    - Wrap the main content area (both calendar and list views) in `DndProvider`
    - Pass `categoryMap` to DndProvider for the DragOverlay task card rendering
    - DndProvider should wrap both views so drag context is shared

    **src/components/calendar/DayCell.tsx:**
    - Wrap each day cell's content area in `DroppableDay` with `dateStr={formatDateKey(date)}`
    - Wrap each TaskCard in `DraggableTask` with the task object
    - Ensure click handlers still work: clicking a TaskCard should open edit modal (not blocked by drag wrapper)
    - The DraggableTask wrapper should pass through onClick to the TaskCard

    **src/components/calendar/CalendarGrid.tsx:**
    - No major changes needed — DayCell handles the DnD wrappers internally
    - Ensure the grid container allows overflow during drag (no `overflow-hidden`)

    **src/components/calendar/WeekView.tsx:**
    - Same as CalendarGrid — DayCell handles DnD internally

    **src/components/list/DayGroup.tsx:**
    - Wrap the entire day group in `DroppableDay` with `dateStr={date}`
    - This makes each day section a valid drop target in list view

    **src/components/list/TaskListItem.tsx:**
    - Wrap in `DraggableTask` with the task object
    - Ensure click (for inline edit) and status toggle still work alongside drag
    - Add a visual drag handle (GripVertical icon from lucide-react) on the left side of each item
    - Drag listeners attached only to the grip handle, NOT the entire item (allows clicking elsewhere for edit/status without accidental drag)

    **src/components/list/ListView.tsx:**
    - Ensure the scroll container doesn't interfere with drag-and-drop
    - No `overflow-hidden` on the scroll container — use `overflow-y-auto` with proper height constraints

    **Visual feedback during drag:**
    - Source cell: item becomes transparent (handled by DraggableTask opacity)
    - Target cell: highlight background (handled by DroppableDay isOver state)
    - DragOverlay: floating copy of the task card following the cursor (handled by DndProvider)

    **Edge cases to handle:**
    - Dropping on the same day = no-op (don't update if date unchanged)
    - Dropping outside any droppable = cancel (automatic with dnd-kit, `over` is null)
    - Drag between calendar view days works naturally since all DayCells are droppables
    - Drag in list view between DayGroups works since all DayGroups are droppables
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero type errors.
    Run `npm run dev` and test drag-and-drop:

    Calendar view:
    1. Create 2-3 tasks on different days
    2. Drag a task from one day to another → task appears in new day, disappears from old day
    3. Verify the task's date updated (check IndexedDB in DevTools)
    4. Drag to same day → no change (no unnecessary update)
    5. Click a task (short click) → edit modal still opens (not blocked by drag)
    6. Drag and drop onto an empty day → task moves there
    7. Valid drop target shows blue highlight during hover

    List view:
    8. Switch to list view
    9. Drag a task using the grip handle from one day to another → task moves
    10. Click a task (not on grip) → inline edit still works
    11. Status toggle click → still works (not intercepted by drag)

    Cross-verification:
    12. Drag a task in calendar view, switch to list view → task shows under new date
    13. Refresh browser → moved tasks persist in their new dates
  </verify>
  <done>
    Drag-to-reschedule works in both calendar and list views. Tasks can be moved between days via drag-and-drop. Visual feedback shows valid drop targets. Click interactions (edit, status toggle) are not affected by drag wrappers. All changes persist via Dexie. TASK-03 complete.
  </done>
</task>

</tasks>

<verification>
- Drag a task between days in calendar view — task moves
- Drag a task between day groups in list view — task moves
- Drop target highlights during drag hover
- DragOverlay shows task preview while dragging
- Click still works for edit (not blocked by drag)
- Status toggle click still works in list view
- Dropping on same day = no-op
- Dropping outside any day = cancel
- Task date updates in IndexedDB after drag
- Both source and target cells update reactively
- Changes persist across browser refresh
- No TypeScript errors
</verification>

<success_criteria>
- TASK-03: User can drag tasks between calendar days to reschedule
- Drag works in calendar view (monthly and weekly)
- Drag works in list view (between day groups)
- Visual feedback: source transparency, target highlight, drag overlay
- Click interactions preserved alongside drag
- Database updated on successful drop
- Reactive updates via useLiveQuery
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-first-foundation/01-05-SUMMARY.md`
</output>
