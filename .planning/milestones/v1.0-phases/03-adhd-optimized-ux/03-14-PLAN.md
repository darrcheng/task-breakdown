---
phase: 03-adhd-optimized-ux
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/list/DayGroup.tsx
autonomous: true
requirements: [TASK-07, ADHD-01]
gap_closure: true

must_haves:
  truths:
    - "Clicking a task in list view opens the full TaskModal (same as calendar view)"
    - "List view has subtask tree, AI breakdown, time estimate — full feature parity"
  artifacts:
    - path: "src/App.tsx"
      provides: "handleTaskClickList opens TaskModal instead of being a no-op"
    - path: "src/components/list/DayGroup.tsx"
      provides: "Task click delegates to onTaskClick prop instead of inline edit"
  key_links:
    - from: "DayGroup.tsx"
      to: "App.tsx handleTaskClickList"
      via: "onTaskClick prop called on task row click"
      pattern: "onTaskClick\\(task\\)"
    - from: "App.tsx"
      to: "TaskModal"
      via: "setModalState opens modal for both calendar and list views"
      pattern: "handleTaskClickList.*setModalState"
---

<objective>
Make list view open TaskModal on task click for full feature parity with calendar view.

Purpose: User reported "no subtask tree in list view, no AI breakdown in list view. List view should have all the same features as calendar view." Root cause: list view uses TaskInlineEdit (bare 46-line wrapper) instead of TaskModal. The simplest fix is to open TaskModal from list view, instantly gaining all features: subtask tree, AI breakdown, time estimates, provider setup, breadcrumb navigation.

Output: Clicking a task in list view opens TaskModal with full features.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/list-view-missing-subtask-breakdown.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire list view task click to open TaskModal</name>
  <files>src/App.tsx, src/components/list/DayGroup.tsx</files>
  <action>
Two changes to connect list view task clicks to the full TaskModal:

**Change 1: App.tsx — Make handleTaskClickList open TaskModal**

Replace the no-op handler (lines 140-143):
```typescript
const handleTaskClickList = (_task: Task) => {
  // List view: handled inline by DayGroup/TaskInlineEdit
  // This is a no-op at App level for list view
};
```

With a handler that opens the modal — same pattern as handleTaskClickCalendar but without clickPosition (list view doesn't need position-relative modals; center the modal instead):
```typescript
const handleTaskClickList = (task: Task) => {
  setModalState({ isOpen: true, date: task.date, task });
};
```

No clickPosition means the modal will use the centered positioning (the else branch in TaskModal's positionStyle on lines 197-203).

**Change 2: DayGroup.tsx — Replace inline edit with onTaskClick delegation**

Currently DayGroup manages editingTaskId state (line 26) and renders TaskInlineEdit when a task is clicked (lines 83-88, line 94). Change this to delegate to the onTaskClick prop instead:

1. Remove the `editingTaskId` state: delete `const [editingTaskId, setEditingTaskId] = useState<number | null>(null);` (line 26)
2. Remove the `TaskInlineEdit` import (line 7)
3. In the task map (lines 82-98), remove the editingTaskId conditional branch. Always render:
```tsx
tasks.map((task) => (
  <DraggableTask key={task.id} task={task}>
    <TaskListItem
      task={task}
      categoryMap={categoryMap}
      onClick={onTaskClick}
    />
  </DraggableTask>
))
```

The `onTaskClick` prop is already declared in DayGroupProps (line 17) but was not being used for actual task clicks. Now pass it through directly to TaskListItem's onClick.

Note: DayGroup's props destructuring (line 20-24) currently omits `onTaskClick` from the destructured variables. Add it back:
```typescript
export function DayGroup({
  date,
  tasks,
  categoryMap,
  onTaskClick,
}: DayGroupProps) {
```

The `onDayClick` prop can remain in the interface but won't be destructured if not used (it isn't used in DayGroup currently — list view inline create is handled via custom DOM event).

After this change, TaskInlineEdit.tsx is no longer imported by any component. It can be left as dead code for now (deletion is a cleanup task, not a bug fix).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    <manual>Switch to list view, click any task — TaskModal opens with full features (subtask tree, Break it down button, time estimate, Someday button). Create subtasks via AI breakdown from list view. Verify drag-and-drop still works in list view.</manual>
  </verify>
  <done>List view opens TaskModal on task click. Subtask tree, AI breakdown, time estimates, and all modal features are accessible from list view — full feature parity with calendar view.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds
- App.tsx: handleTaskClickList calls setModalState (not a no-op)
- DayGroup.tsx: no editingTaskId state, no TaskInlineEdit import, onTaskClick prop passed to TaskListItem
</verification>

<success_criteria>
- Clicking a task in list view opens the centered TaskModal
- TaskModal in list view has: subtask tree, Break it Down button, time estimate, Someday button, breadcrumb navigation
- Drag-and-drop in list view still works (DraggableTask wrapping is preserved)
- Inline create (Enter key, + button) still works independently
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-14-SUMMARY.md`
</output>
