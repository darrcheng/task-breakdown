---
phase: 03-adhd-optimized-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/db/database.ts
  - src/db/hooks.ts
  - src/components/list/TaskListItem.tsx
  - src/components/task/SubtaskList.tsx
autonomous: true
requirements:
  - TASK-07
  - ADHD-03
  - ADHD-05

must_haves:
  truths:
    - "Completing a task shows a satisfying glow + strikethrough + fade animation"
    - "Completing a subtask shows the same celebration animation"
    - "First incomplete subtask has a visible accent ring distinguishing it from siblings"
    - "Start-here ring moves to next incomplete subtask after one is completed"
    - "Dexie v3 migration succeeds without data loss — existing tasks retain all prior fields"
  artifacts:
    - path: "src/types/index.ts"
      provides: "EnergyLevel type, isSomeday and timeEstimate fields on Task"
      contains: "EnergyLevel"
    - path: "src/db/database.ts"
      provides: "Dexie v3 schema with energyLevel index and upgrade migration"
      contains: "version(3)"
    - path: "src/db/hooks.ts"
      provides: "useOverdueTasks and updated query hooks with isSomeday filter"
      contains: "useOverdueTasks"
    - path: "src/components/list/TaskListItem.tsx"
      provides: "Enhanced departure animation with emerald ring glow"
      contains: "ring-2 ring-emerald-400"
    - path: "src/components/task/SubtaskList.tsx"
      provides: "Start-here ring on first incomplete subtask and enhanced SubtaskRow departure"
      contains: "isStartHere"
  key_links:
    - from: "src/db/database.ts"
      to: "src/types/index.ts"
      via: "Task type import for v3 migration"
      pattern: "energyLevel.*isSomeday"
    - from: "src/components/task/SubtaskList.tsx"
      to: "useSubtasks result"
      via: "firstIncompleteIndex computation"
      pattern: "findIndex.*status.*done"
---

<objective>
Extend the data model for Phase 3 features (energy, time estimates, someday) and enhance the existing departure animation into a satisfying celebration, plus add start-here visual highlighting on first incomplete subtasks.

Purpose: Establishes the schema foundation that all Phase 3 plans depend on, and delivers two quick visual wins (celebration glow + start-here ring) that require no new components.

Output: Dexie v3 migration with new Task fields, enhanced celebration animation on TaskListItem and SubtaskRow, start-here ring on SubtaskList.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adhd-optimized-ux/03-CONTEXT.md
@.planning/phases/03-adhd-optimized-ux/03-RESEARCH.md
@src/types/index.ts
@src/db/database.ts
@src/db/hooks.ts
@src/components/list/TaskListItem.tsx
@src/components/task/SubtaskList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Task type and Dexie v3 schema migration</name>
  <files>src/types/index.ts, src/db/database.ts, src/db/hooks.ts</files>
  <action>
In `src/types/index.ts`:
- Add `export type EnergyLevel = 'low' | 'medium' | 'high';` after the existing type exports.
- Add these optional fields to the `Task` interface:
  - `energyLevel?: EnergyLevel | null;` — energy tag
  - `timeEstimate?: number | null;` — AI-generated estimate in minutes
  - `timeEstimateOverride?: number | null;` — user-set override in minutes
  - `isSomeday?: boolean;` — true = archived to Someday list, not on calendar
- Update `ViewMode` type to `'calendar' | 'list' | 'someday'` to support the Someday view.

In `src/db/database.ts`:
- Add a `db.version(3).stores({...}).upgrade(...)` block after the existing v2 block.
- v3 stores string for tasks: `'++id, date, status, categoryId, parentId, depth, energyLevel'` — adds `energyLevel` as an indexed field for filter queries.
- categories and aiSettings store strings remain unchanged from v2.
- In the `.upgrade()` callback, modify all existing tasks to set default values:
  - `energyLevel` → `null`
  - `timeEstimate` → `null`
  - `timeEstimateOverride` → `null`
  - `isSomeday` → `false`

In `src/db/hooks.ts`:
- Update `useTasksByDate` to filter out `isSomeday` tasks: add `.filter((t) => !t.isSomeday && ...)` to both the showCompleted and non-showCompleted branches. Also filter out subtasks (parentId undefined or null) since only root tasks appear in calendar/list views — wait, check existing behavior: the current hook does NOT filter by parentId. Leave parentId filtering as-is (it's handled elsewhere).
- Actually, just add `!t.isSomeday` to the filter. For the showCompleted branch that currently returns all, wrap it in a filter: `return query.filter((t) => !t.isSomeday).toArray();`.
- Update `useTasksByDateRange` identically — add `!t.isSomeday` filter to both branches.
- Add a new `useOverdueTasks` hook:
  ```typescript
  export function useOverdueTasks() {
    const today = formatDateKey(new Date());
    return useLiveQuery(
      () =>
        db.tasks
          .where('date')
          .below(today)
          .filter((t) => t.status !== 'done' && !t.isSomeday && !t.parentId)
          .toArray(),
      [today],
    );
  }
  ```
  Import `formatDateKey` from `'../utils/dates'`.
- Add a new `useSomedayTasks` hook:
  ```typescript
  export function useSomedayTasks() {
    return useLiveQuery(
      () => db.tasks.filter((t) => !!t.isSomeday && !t.parentId).toArray(),
    );
  }
  ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Build succeeds, no type errors from the new fields</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Task type has EnergyLevel, timeEstimate*, isSomeday fields. ViewMode includes 'someday'. Dexie v3 migration upgrades existing data. useOverdueTasks and useSomedayTasks hooks exist. useTasksByDate/Range filter out someday tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance celebration animation on TaskListItem and SubtaskRow</name>
  <files>src/components/list/TaskListItem.tsx, src/components/task/SubtaskList.tsx</files>
  <action>
Per locked decisions: subtle & satisfying, no sound, visual only — strikethrough + fade + glow. Enhance the existing `departing` CSS classes.

In `src/components/list/TaskListItem.tsx`:
- Find the `departing &&` class string in the outer div's `clsx()` call.
- Replace the current departure classes with:
  `departing && 'ring-2 ring-emerald-400 ring-offset-1 line-through decoration-green-600 text-green-600 opacity-0 transition-all duration-[1500ms]'`
- This adds a brief emerald glow ring that appears immediately when departing starts, while the opacity fades to 0 over 1500ms. The ring + strikethrough + fade together create a satisfying "completed" moment.
- The 1500ms timing matches the existing departureTimeout — no timing changes needed.

In `src/components/task/SubtaskList.tsx` (SubtaskRow component):
- Find the `departing &&` class in the outer div's `clsx()` call (currently `departing && 'opacity-50'`).
- Replace with: `departing && 'ring-2 ring-emerald-400 ring-offset-1 opacity-0 transition-all duration-[1500ms]'`
- Find the title button's departing branch: `departing ? 'text-green-600 line-through'` — keep this as-is, it already handles the text styling.
- The combination gives SubtaskRow the same glow + fade as TaskListItem.

Both components already have the `departing` state machine with 1500ms timeout. No logic changes needed — only CSS class enhancements.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Complete a task in list view — should see emerald ring glow appear then fade out over 1.5s with strikethrough</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>TaskListItem and SubtaskRow show ring-2 ring-emerald-400 glow + strikethrough + fade-out on departure. No sound. Same treatment for all completions (no tiered celebrations).</done>
</task>

<task type="auto">
  <name>Task 3: Add start-here ring highlight on first incomplete subtask</name>
  <files>src/components/task/SubtaskList.tsx</files>
  <action>
Per locked decisions: first subtask gets accent border/subtle glow, no text label, purely visual, auto-applied to first incomplete subtask.

In `src/components/task/SubtaskList.tsx`:

1. In the `SubtaskList` component, before the `return`, compute `firstIncompleteIndex`:
   ```typescript
   const firstIncompleteIndex = subtasks.findIndex((s) => s.status !== 'done');
   ```

2. In the `.map()` rendering subtasks, pass a new prop to SubtaskRow:
   ```typescript
   subtasks.map((subtask, index) => (
     <SubtaskRow
       key={subtask.id}
       subtask={subtask}
       parentDepth={parentDepth}
       categoryMap={categoryMap}
       onOpenSubtask={onOpenSubtask}
       isStartHere={index === firstIncompleteIndex}
     />
   ))
   ```

3. Add `isStartHere` to `SubtaskRowProps` interface:
   ```typescript
   interface SubtaskRowProps {
     subtask: Task;
     parentDepth: number;
     categoryMap?: Map<number, Category>;
     onOpenSubtask: (task: Task) => void;
     isStartHere?: boolean;
   }
   ```

4. Destructure `isStartHere` in SubtaskRow function params.

5. In SubtaskRow's outer div `clsx()`, add a condition for the start-here ring. It should NOT apply when the subtask is departing:
   ```typescript
   isStartHere && !departing && 'ring-2 ring-violet-400 ring-offset-1 rounded-md'
   ```
   Use violet-400 to visually distinguish from the emerald celebration ring.

The ring will automatically move to the next incomplete subtask when one is completed, because `useSubtasks` is reactive via `useLiveQuery` and `findIndex` recomputes on each render. During the 1500ms departure window, the departing item still has `status !== 'done'` in the DB, so the ring stays on it — which is correct UX (ring stays during celebration, then moves after DB write).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Open a task with subtasks — first incomplete subtask should have a violet ring border; complete it and ring should move to next incomplete</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>First incomplete subtask in any SubtaskList has a ring-2 ring-violet-400 accent ring. No text label. Ring auto-moves when subtasks are completed. Ring hidden during departure animation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- Dexie v3 migration block exists in database.ts with energyLevel index
- Task type includes energyLevel, timeEstimate, timeEstimateOverride, isSomeday
- ViewMode includes 'someday'
- useOverdueTasks and useSomedayTasks hooks exist in hooks.ts
- useTasksByDate and useTasksByDateRange filter out isSomeday tasks
- TaskListItem departure has ring-2 ring-emerald-400
- SubtaskRow departure has ring-2 ring-emerald-400
- SubtaskRow start-here has ring-2 ring-violet-400 on first incomplete
</verification>

<success_criteria>
- TypeScript compiles clean
- Build succeeds
- Schema migration adds all new fields with safe defaults
- Celebration animation visually enhanced with glow ring
- Start-here highlighting visible on first incomplete subtask
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-01-SUMMARY.md`
</output>
