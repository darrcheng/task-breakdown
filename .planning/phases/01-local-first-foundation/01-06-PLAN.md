---
phase: 01-local-first-foundation
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - src/components/ui/EmptyState.tsx
  - src/components/ui/CategoryManager.tsx
  - src/App.tsx
  - src/app.css
autonomous: false
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05
  - TASK-06
  - PLAT-03
must_haves:
  truths:
    - "Empty state shows clean view with hint text when no tasks exist"
    - "User can manage categories (view defaults, add custom, edit)"
    - "All 7 requirements are demonstrably working end-to-end"
    - "Data survives browser close and reopen"
  artifacts:
    - path: "src/components/ui/EmptyState.tsx"
      provides: "Empty state component with hint text"
      min_lines: 10
    - path: "src/components/ui/CategoryManager.tsx"
      provides: "Category management interface"
      min_lines: 30
  key_links:
    - from: "src/components/ui/CategoryManager.tsx"
      to: "src/db/database.ts"
      via: "db.categories.add/update for custom categories"
      pattern: "db\\.categories"
    - from: "src/App.tsx"
      to: "src/components/ui/EmptyState.tsx"
      via: "shown when task count is zero"
      pattern: "EmptyState"
---

<objective>
Polish the application with empty state, category management, and final integration verification across all Phase 1 requirements.

Purpose: Ensure all 7 requirements (TASK-01 through TASK-06, PLAT-03) are demonstrably working. Add missing polish: empty state experience, custom category management, and visual refinements.
Output: EmptyState component, CategoryManager, polished UI, all requirements verified.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-first-foundation/01-RESEARCH.md
@.planning/phases/01-local-first-foundation/01-CONTEXT.md
@.planning/phases/01-local-first-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EmptyState, CategoryManager, and apply visual polish</name>
  <files>
    src/components/ui/EmptyState.tsx
    src/components/ui/CategoryManager.tsx
    src/App.tsx
    src/app.css
  </files>
  <action>
    **src/components/ui/EmptyState.tsx:**
    - Shown when the database has zero tasks
    - Clean, minimal design matching the app aesthetic
    - Subtle hint text: "Click a day to add your first task"
    - Optional: small calendar icon or illustration using lucide-react (CalendarDays icon)
    - Light gray text, centered, non-intrusive
    - Props: `viewMode: ViewMode` (to customize hint: "Click a day" for calendar, "Click + to add" for list)

    **src/components/ui/CategoryManager.tsx:**
    - Accessible from the app header (e.g., a Settings/Tag icon button that opens a panel or modal)
    - Displays all categories (default + custom) with their icons
    - "Add Category" form: name input + icon selector (dropdown of available lucide icons — provide a curated list of ~20 suitable icons)
    - Edit existing category: click to edit name or icon
    - Default categories can be edited but not deleted (isDefault: true flag)
    - Custom categories can be deleted (with confirmation, and warning if tasks use that category)
    - Uses `db.categories.add()`, `db.categories.update()`, `db.categories.delete()`
    - useLiveQuery keeps the list reactive

    **src/App.tsx updates:**
    - Integrate EmptyState: show when total task count is 0 (use useLiveQuery to count)
    - Add category manager trigger button in header (Tag icon from lucide-react)
    - State: `isCategoryManagerOpen: boolean`
    - Render CategoryManager as a modal or side panel

    **src/app.css polish:**
    - Add any custom Tailwind utilities needed via `@layer`
    - Ensure consistent spacing, font sizes, and color palette across the app
    - Smooth transitions for: modal open/close, inline edit expand/collapse, status color changes, show/hide completed toggle
    - Focus-visible rings for keyboard navigation accessibility
    - Responsive layout: app should work well at different browser widths (min-width ~320px for future mobile, max comfortable at 1200px+)

    **General polish pass:**
    - Ensure all clickable elements have cursor-pointer
    - Ensure hover states on interactive elements
    - Ensure keyboard accessibility: Tab through interactive elements, Enter/Space to activate
    - Ensure the header layout is clean: app title left, controls right (view toggle, show completed toggle, category manager button)
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero type errors.
    Run `npm run dev` and verify:
    1. Fresh database (clear IndexedDB) → empty state shows with hint text
    2. Create one task → empty state disappears, task shows in calendar
    3. Category manager opens, shows 5 default categories
    4. Add a custom category → appears in category list and in task form dropdown
    5. Edit a default category name → updates everywhere
    6. Delete a custom category → removed
    7. Tab through the UI → focus rings visible on all interactive elements
    8. App looks good at 1200px wide and at 375px wide (mobile-ish)
  </verify>
  <done>
    Empty state provides a clean first-run experience. Category manager allows viewing, adding, editing, and deleting categories. App is visually polished with consistent spacing, transitions, and hover/focus states.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 1 requirements end-to-end</name>
  <files>src/App.tsx</files>
  <action>
    Human verification checkpoint. Complete Phase 1: Local-First Foundation application with all 7 requirements implemented:
    - TASK-01: Quick capture (modal in calendar, inline in list)
    - TASK-02: Calendar view with monthly grid, week toggle, month navigation
    - TASK-03: Drag-to-reschedule between days in both views
    - TASK-04: Inline editing (modal in calendar, expand in list)
    - TASK-05: Task deletion with confirmation
    - TASK-06: Mark as done (status toggle, hide/show completed)
    - PLAT-03: Data persists via IndexedDB/Dexie
  </action>
  <verify>
    Open the app at the Vite dev server URL (http://localhost:5173).

    **TASK-01 — Quick Capture:**
    1. In calendar view, click an empty day — modal opens
    2. Type a task title, select a category — create task
    3. Switch to list view, click "+" on a day — inline input appears
    4. Type title, press Enter — task created

    **TASK-02 — Calendar View:**
    5. Monthly grid shows correct days with task cards
    6. Click prev/next arrows — month changes
    7. Click "Today" — returns to current month
    8. Toggle to week view — shows 7 days only
    9. Day cells expand to fit multiple tasks (no truncation)

    **TASK-03 — Drag to Reschedule:**
    10. In calendar, drag a task to a different day — it moves
    11. In list view, drag via grip handle to another day — it moves
    12. Drop target highlights during drag

    **TASK-04 — Edit Tasks:**
    13. In calendar, click a task — modal opens with data pre-filled
    14. Change title and category — save — changes reflected
    15. In list view, click task — inline edit expands
    16. Edit description — save — changes reflected

    **TASK-05 — Delete Tasks:**
    17. Open a task for editing, click delete — confirm — task gone

    **TASK-06 — Mark as Done:**
    18. Toggle a task status to "done" — task disappears from view
    19. Toggle "Show completed" — done task reappears
    20. Toggle "Show completed" off — done task hides again

    **PLAT-03 — Data Persistence:**
    21. Close the browser tab completely
    22. Reopen the app — all tasks still present with correct dates/statuses
    23. Hard refresh (Ctrl+Shift+R) — data persists

    **Category Management:**
    24. Open category manager — 5 defaults shown
    25. Add a custom category — available in task forms
    26. Edit a category — changes reflect on existing task cards

    Verify each requirement passes. Report any issues or approve.
  </verify>
  <done>All 7 Phase 1 requirements verified and approved by user. Phase 1 Local-First Foundation is complete.</done>
</task>

</tasks>

<verification>
- Empty state shows on fresh database
- Category management works (add, edit, delete custom)
- All 7 requirements verified end-to-end
- UI is polished with consistent design
- Keyboard accessible
- Data persists across browser sessions
</verification>

<success_criteria>
- TASK-01: Quick capture works in both views
- TASK-02: Calendar view with monthly grid, week toggle, navigation
- TASK-03: Drag-to-reschedule in both views
- TASK-04: Edit tasks in both views
- TASK-05: Delete tasks with confirmation
- TASK-06: Mark as done, hide/show completed
- PLAT-03: Data persists across browser sessions
- Empty state experience for new users
- Category management for customization
- Visual polish and accessibility
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-first-foundation/01-06-SUMMARY.md`
</output>
