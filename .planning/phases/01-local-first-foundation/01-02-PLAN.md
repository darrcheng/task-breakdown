---
phase: 01-local-first-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/App.tsx
  - src/components/calendar/CalendarGrid.tsx
  - src/components/calendar/DayCell.tsx
  - src/components/calendar/MonthNavigation.tsx
  - src/components/calendar/WeekView.tsx
  - src/components/task/TaskCard.tsx
  - src/components/ui/ViewToggle.tsx
autonomous: true
requirements:
  - TASK-02
must_haves:
  truths:
    - "User can view tasks organized by day in a monthly calendar grid"
    - "User can navigate between months with previous/next controls"
    - "User can toggle between monthly and weekly calendar views"
    - "Today's date is visually highlighted in the grid"
    - "Tasks appear as colored cards in their assigned day cell"
    - "Day cells expand vertically to fit all tasks without truncation"
  artifacts:
    - path: "src/components/calendar/CalendarGrid.tsx"
      provides: "Monthly calendar grid with 7-column CSS grid layout"
      min_lines: 40
    - path: "src/components/calendar/DayCell.tsx"
      provides: "Individual day cell rendering task cards"
      min_lines: 25
    - path: "src/components/calendar/MonthNavigation.tsx"
      provides: "Month selector with prev/next and today button"
      min_lines: 20
    - path: "src/components/task/TaskCard.tsx"
      provides: "Task card with category icon, title, and status color"
      min_lines: 20
  key_links:
    - from: "src/components/calendar/DayCell.tsx"
      to: "src/db/hooks.ts"
      via: "useTasksByDate hook"
      pattern: "useTasksByDate"
    - from: "src/components/task/TaskCard.tsx"
      to: "src/utils/categories.ts"
      via: "STATUS_COLORS and CATEGORY_ICONS"
      pattern: "STATUS_COLORS|CATEGORY_ICONS"
    - from: "src/App.tsx"
      to: "src/components/calendar/CalendarGrid.tsx"
      via: "renders calendar as default view"
      pattern: "CalendarGrid"
---

<objective>
Build the calendar view with monthly grid, week view toggle, month navigation, and task card rendering.

Purpose: TASK-02 requires users to view tasks organized by day in a calendar view. This is the primary view of the application.
Output: CalendarGrid, DayCell, MonthNavigation, WeekView, TaskCard components rendered in App.tsx.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-first-foundation/01-RESEARCH.md
@.planning/phases/01-local-first-foundation/01-CONTEXT.md
@.planning/phases/01-local-first-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CalendarGrid, DayCell, MonthNavigation, and WeekView components</name>
  <files>
    src/components/calendar/CalendarGrid.tsx
    src/components/calendar/DayCell.tsx
    src/components/calendar/MonthNavigation.tsx
    src/components/calendar/WeekView.tsx
    src/components/ui/ViewToggle.tsx
  </files>
  <action>
    **src/components/calendar/MonthNavigation.tsx:**
    - Displays current month/year (e.g., "February 2026") with left/right arrow buttons to navigate months
    - "Today" button that resets to current month
    - Toggle between "Month" and "Week" view (CalendarView type)
    - Props: `currentMonth: Date`, `onMonthChange: (date: Date) => void`, `calendarView: CalendarView`, `onCalendarViewChange: (view: CalendarView) => void`
    - Use ChevronLeft, ChevronRight from lucide-react
    - Tailwind: flex layout, rounded buttons, hover states

    **src/components/calendar/CalendarGrid.tsx:**
    - Uses `getCalendarDays(month)` from utils/dates.ts to compute the grid
    - Renders a 7-column CSS grid (`grid grid-cols-7`)
    - Row header: Sun Mon Tue Wed Thu Fri Sat (abbreviated day names)
    - Each cell is a DayCell component
    - Uses `grid-auto-rows: minmax(min-content, auto)` so rows expand to fit content (per user requirement: no "+N more" truncation)
    - Props: `month: Date`, `showCompleted: boolean`, `onDayClick: (date: string) => void`, `onTaskClick: (task: Task) => void`

    **src/components/calendar/WeekView.tsx:**
    - Similar to CalendarGrid but only renders 7 days (one week)
    - Uses `getWeekDays(currentDate)` from utils/dates.ts
    - Same 7-column grid layout, taller cells since only 1 row
    - Props: same as CalendarGrid

    **src/components/calendar/DayCell.tsx:**
    - Displays day number with dimmed style for days outside current month
    - Highlights today with a distinct background (e.g., blue-50 border)
    - Queries tasks for its date using `useTasksByDate(dateStr, showCompleted)`
    - Renders TaskCard for each task
    - Clickable: calls `onDayClick` when empty area is clicked (for task creation in Plan 04)
    - Props: `date: Date`, `isCurrentMonth: boolean`, `showCompleted: boolean`, `onDayClick: (date: string) => void`, `onTaskClick: (task: Task) => void`
    - Min height so empty days are still clickable, but auto-expands for content

    **src/components/ui/ViewToggle.tsx:**
    - Toggle between 'calendar' and 'list' ViewMode
    - Two-button group: Calendar icon + List icon from lucide-react
    - Active state highlighted
    - Props: `viewMode: ViewMode`, `onViewModeChange: (mode: ViewMode) => void`
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero type errors.
    Visual check: Components should be importable and render without runtime errors.
  </verify>
  <done>
    CalendarGrid renders a 7-column monthly grid with day numbers. DayCell shows tasks for each day. MonthNavigation provides month/year display with prev/next/today buttons and month/week toggle. WeekView shows a single week. ViewToggle switches between calendar and list modes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build TaskCard component and wire calendar view into App.tsx</name>
  <files>
    src/components/task/TaskCard.tsx
    src/App.tsx
  </files>
  <action>
    **src/components/task/TaskCard.tsx:**
    - Displays a single task as a small card
    - Shows: category icon (from lucide-react via CATEGORY_ICONS mapping) + task title
    - Card background/border color based on task status using STATUS_COLORS from categories.ts
    - Compact size for calendar cells: single line, small text, rounded corners
    - Clickable: calls `onTaskClick` prop (for edit modal in Plan 04)
    - Props: `task: Task`, `categoryMap: Map<number, Category>`, `onClick?: (task: Task) => void`
    - Use `clsx` for conditional class composition
    - Truncate long titles with `truncate` Tailwind class in calendar view
    - Look up category by task.categoryId from categoryMap; fall back to a default icon if not found

    **src/App.tsx:**
    - State: `currentMonth` (Date), `calendarView` ('month' | 'week'), `viewMode` ('calendar' | 'list'), `showCompleted` (boolean)
    - Layout: Header with app title "TaskBreaker", ViewToggle, show/hide completed toggle
    - Below header: MonthNavigation (visible when viewMode is 'calendar')
    - Main content: CalendarGrid or WeekView depending on calendarView state (when viewMode is 'calendar')
    - When viewMode is 'list', render a placeholder div "List view coming soon" (implemented in Plan 03)
    - Show/hide completed: a toggle button with Eye/EyeOff icon from lucide-react, controls `showCompleted` state passed down to DayCell
    - Empty state: When no tasks exist, the calendar should look clean with day cells showing just numbers. DayCell should display a subtle hover hint on empty days.

    Wire up the `onDayClick` and `onTaskClick` handlers as no-ops for now (will be connected to modals in Plan 04). Log to console for verification.

    Manually add 2-3 test tasks to IndexedDB via browser DevTools console to verify rendering:
    ```javascript
    // Test in browser console:
    // import { db } from './src/db/database';
    // db.tasks.add({ title: 'Test task', description: '', date: '2026-02-22', status: 'todo', categoryId: 1, createdAt: new Date(), updatedAt: new Date() });
    ```
    Include a comment in App.tsx with these test commands for manual verification.
  </action>
  <verify>
    Run `npx tsc --noEmit` — zero type errors.
    Run `npm run dev` — app renders calendar grid.
    Visual verification in browser:
    1. Monthly calendar grid shows correct days for current month
    2. Navigating months with arrows works (February -> March -> February)
    3. "Today" button returns to current month
    4. Today's date has distinct visual highlight
    5. Switching to week view shows only 7 days
    6. Days outside current month appear dimmed
    7. Empty day cells are still visible and have minimum height
    8. Show/hide completed toggle exists in header
    9. ViewToggle shows calendar/list buttons (list shows placeholder)
  </verify>
  <done>
    Calendar view renders a monthly grid with correct dates, navigable months, today highlighted, week view toggle working, task cards display with status colors and category icons, day cells expand to fit content. Users can view tasks organized by day in the calendar (TASK-02).
  </done>
</task>

</tasks>

<verification>
- Calendar grid displays correct number of days for each month
- Month navigation changes the displayed month
- Today button returns to current month
- Today's date is visually distinct
- Week view shows exactly 7 days
- Task cards show category icon + title with status-based colors
- Day cells expand to accommodate multiple tasks (no overflow/truncation)
- Show/hide completed toggle is present
- Calendar/List view toggle is present
- No TypeScript errors
</verification>

<success_criteria>
- Monthly calendar grid renders correctly for any month
- Month-to-month navigation with prev/next arrows and today button
- Month/week view toggle switches between full month and single week
- TaskCard renders with category icon, title, and status color
- DayCell queries and displays tasks for its date reactively
- Day cells auto-expand vertically (no "+N more" truncation)
- Today visually highlighted
- Out-of-month days dimmed
- Calendar/list view toggle in header
- Show/hide completed toggle in header
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-first-foundation/01-02-SUMMARY.md`
</output>
