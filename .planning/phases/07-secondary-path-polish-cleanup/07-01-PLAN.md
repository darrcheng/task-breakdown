---
phase: 07-secondary-path-polish-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/task/TaskInlineCreate.tsx
  - src/App.tsx
  - src/components/ui/SettingsModal.tsx
  - src/components/task/TaskInlineEdit.tsx
autonomous: true
requirements: [ADHD-02]

must_haves:
  truths:
    - "Tasks created via inline create receive AI time estimates"
    - "showKeyboardShortcuts toggle controls keyboard handler in App.tsx"
    - "TaskInlineEdit dead code is removed"
  artifacts:
    - path: "src/components/task/TaskInlineCreate.tsx"
      provides: "Inline create with fire-and-forget AI time estimation"
      contains: "triggerEstimate"
    - path: "src/App.tsx"
      provides: "Keyboard handler gated by showKeyboardShortcuts setting"
      contains: "settings.showKeyboardShortcuts"
    - path: "src/components/ui/SettingsModal.tsx"
      provides: "Renamed setting label reflecting functional control"
      contains: "Enable Keyboard Shortcuts"
  key_links:
    - from: "src/components/task/TaskInlineCreate.tsx"
      to: "src/hooks/useTimeEstimate.ts"
      via: "useTimeEstimate hook import and triggerEstimate call after db.tasks.add"
      pattern: "triggerEstimate\\(newId"
    - from: "src/App.tsx"
      to: "src/hooks/useSettings.ts"
      via: "settings.showKeyboardShortcuts guard in keyboard useEffect"
      pattern: "settings\\.showKeyboardShortcuts"
---

<objective>
Wire triggerEstimate to inline create, connect keyboard shortcuts toggle, delete TaskInlineEdit dead code.

Purpose: Close three secondary-path gaps (FLOW-01, INT-03, FLOW-02) — inline-created tasks get AI time estimates, the keyboard shortcuts toggle actually enables/disables shortcuts, and confirmed dead code is removed.
Output: Three targeted file modifications + one file deletion.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/components/task/TaskInlineCreate.tsx
@src/hooks/useTimeEstimate.ts
@src/App.tsx
@src/hooks/useSettings.ts
@src/components/ui/SettingsModal.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hooks/useTimeEstimate.ts:
```typescript
export function useTimeEstimate(): {
  triggerEstimate: (taskId: number, title: string, description: string, categoryId: number) => void;
};
```

From src/hooks/useSettings.ts:
```typescript
export interface Settings {
  weekStartsOn: 0 | 1;
  showKeyboardShortcuts: boolean;
}
export function useSettings(): {
  settings: Settings;
  updateSettings: (updates: Partial<Settings>) => void;
};
```

From src/db/database.ts (Dexie):
```typescript
// db.tasks.add() returns Promise<IndexableType> — cast to number for triggerEstimate
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AI time estimation to TaskInlineCreate</name>
  <files>src/components/task/TaskInlineCreate.tsx</files>
  <action>
1. Add import: `import { useTimeEstimate } from '../../hooks/useTimeEstimate';`
2. Inside the component, add the hook call: `const { triggerEstimate } = useTimeEstimate();`
3. In `handleSubmit`, after the `await db.tasks.add(...)` call (which currently stores the result implicitly), capture the returned ID:
   - Change `await db.tasks.add({...})` to `const newId = await db.tasks.add({...});`
4. Immediately after the db.tasks.add line, add the fire-and-forget estimation call:
   `triggerEstimate(newId as number, title.trim(), '', finalCategoryId);`
   - Use `title.trim()` (same value passed to db.tasks.add)
   - Use empty string `''` for description (inline create has no description field)
   - Use `finalCategoryId` (the variable already computed on line 23, BEFORE the setCategoryId(0) reset)
   - Cast newId as number (same pattern as TaskModal line 130)
5. Do NOT await triggerEstimate — it is intentionally fire-and-forget
6. Do NOT change any other behavior (title reset, categoryId reset, focus management)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Verify: TypeScript compiles without errors. Grep for `triggerEstimate` in TaskInlineCreate.tsx confirms the call exists.
  </verify>
  <done>TaskInlineCreate calls triggerEstimate after every successful db.tasks.add with the new task's ID, title, empty description, and selected categoryId. Fire-and-forget pattern matches TaskModal.</done>
</task>

<task type="auto">
  <name>Task 2: Connect keyboard shortcuts toggle to keyboard handler</name>
  <files>src/App.tsx, src/components/ui/SettingsModal.tsx</files>
  <action>
**In src/App.tsx:**
1. Find the keyboard shortcuts useEffect (starts at approximately line 62 with comment `// Global keyboard shortcuts`)
2. Add a new guard immediately after the existing `if (isMobile) return;` line:
   `if (!settings.showKeyboardShortcuts) return;`
   This completely disables all keyboard shortcuts when the toggle is OFF. When the setting is false, no event listener is added at all.
3. Add `settings.showKeyboardShortcuts` to the dependency array of this useEffect. The current deps are:
   `[isMobile, calendarView, viewMode, modalState.isOpen, isCategoryManagerOpen, isSettingsOpen, isQuickPickerOpen]`
   Add `settings.showKeyboardShortcuts` after `isMobile`:
   `[isMobile, settings.showKeyboardShortcuts, calendarView, viewMode, modalState.isOpen, isCategoryManagerOpen, isSettingsOpen, isQuickPickerOpen]`

**In src/components/ui/SettingsModal.tsx:**
1. Find the heading text `Keyboard shortcuts` (approximately line 100)
2. Change it to `Enable Keyboard Shortcuts` to reflect that the toggle controls functional behavior, not just visual hints
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Verify: TypeScript compiles. Grep for `settings.showKeyboardShortcuts` in App.tsx shows it appears in both the guard and the dependency array. Grep for `Enable Keyboard Shortcuts` in SettingsModal.tsx confirms label rename.
  </verify>
  <done>When showKeyboardShortcuts is false, the keydown event listener is never registered in App.tsx (no shortcuts work). When true, all shortcuts work as before. SettingsModal heading reads "Enable Keyboard Shortcuts".</done>
</task>

<task type="auto">
  <name>Task 3: Remove TaskInlineEdit dead code</name>
  <files>src/components/task/TaskInlineEdit.tsx</files>
  <action>
1. Verify no file in src/ imports TaskInlineEdit:
   `grep -r "TaskInlineEdit" src/ --include="*.tsx" --include="*.ts" | grep -v "TaskInlineEdit.tsx"`
   This should return zero results (already verified during research — no imports exist).
2. Delete the file: `rm src/components/task/TaskInlineEdit.tsx`
3. Run TypeScript compilation to confirm no broken imports: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    Verify: TypeScript compiles without errors. `ls src/components/task/TaskInlineEdit.tsx` returns "No such file".
  </verify>
  <done>TaskInlineEdit.tsx is deleted. No TypeScript compilation errors. No broken imports anywhere in the codebase.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `npx tsc --noEmit` returns 0 errors
2. TaskInlineCreate.tsx contains `triggerEstimate` call after `db.tasks.add`
3. App.tsx keyboard useEffect has `settings.showKeyboardShortcuts` guard and in dependency array
4. SettingsModal.tsx heading reads "Enable Keyboard Shortcuts"
5. TaskInlineEdit.tsx does not exist
6. Dev server starts without errors: `npm run dev` (quick smoke test)
</verification>

<success_criteria>
- [ ] Inline-created tasks trigger AI time estimation (fire-and-forget)
- [ ] Keyboard shortcuts toggle disables/enables all keyboard shortcuts
- [ ] SettingsModal label says "Enable Keyboard Shortcuts"
- [ ] TaskInlineEdit.tsx deleted with no broken imports
- [ ] TypeScript compilation passes
- [ ] No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/07-secondary-path-polish-cleanup/07-01-SUMMARY.md`
</output>
