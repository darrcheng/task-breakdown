---
status: diagnosed
phase: 01.1-ui-polish-implement-12-enhancement-todos-from-phase-1-uat
source: 01.1-01-SUMMARY.md, 01.1-02-SUMMARY.md, 01.1-03-SUMMARY.md, 01.1-04-SUMMARY.md, 01.1-05-SUMMARY.md
started: 2026-02-22T12:00:00Z
updated: 2026-02-22T13:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Today Red Circle Indicator
expected: In calendar view, today's date shows a red filled circle with white text (Google Calendar-style).
result: pass

### 2. Delete Default Categories
expected: Hovering over any category (including the 5 defaults) in Category Manager shows a delete button. Clicking it removes the category.
result: pass

### 3. Done Task Two-Click Reopen Safety
expected: Clicking a done task's status once shows a pulsing amber ring indicator. A second click within 2 seconds cycles it back to todo. If you wait longer than 2 seconds, it resets and no status change occurs.
result: issue
reported: "once task is marked as done, it disappears. I wanted a delay before it disappears. I don't want this pulsing amber ring."
severity: major

### 4. Status Pill Buttons in Task Form
expected: When creating/editing a task in the modal form, status selection shows three colored clickable pill buttons (todo, in-progress, done) instead of a dropdown.
result: pass

### 5. Category Combobox with Search
expected: In the task modal form, the category field is a searchable combobox. Typing filters the list. Keyboard navigation (arrow keys, Enter) works to select a category.
result: issue
reported: "the dropdown is not empty by default. And once the category is selected the icon doesn't show"
severity: major

### 6. Inline Category Creation from Combobox
expected: In the task form combobox, typing a category name that doesn't exist shows a "Create [name]" option. Selecting it creates the category and assigns it.
result: pass

### 7. Recent Categories First
expected: Categories you've recently selected appear at the top of the category combobox list. After selecting a category on a task, it should appear first next time you open the combobox.
result: pass

### 8. Larger Add Button in List View
expected: In list view, each day header shows a prominent circular add-task button (not just a small icon). It has a visible background and blue hover state.
result: issue
reported: "the + mark is not centered in the circle"
severity: cosmetic

### 9. Blank Space Click to Create
expected: In list view, clicking the blank space below existing tasks in a day group opens the inline task creation form. Empty days show "Click to add a task" hint text.
result: pass

### 10. Category in Inline Task Create
expected: The inline task creation form (in list view) includes a compact category selector dropdown next to the title input.
result: issue
reported: "pass, but can you make this field slightly larger? and the task field slightly smaller?"
severity: cosmetic

### 11. Sticky Header and Navigation
expected: When scrolling down in calendar view, the app header and month/week navigation bar remain fixed at the top of the viewport.
result: pass

### 12. Full Calendar Cell Click Target
expected: Clicking anywhere in a calendar day cell (not just the add button) opens the task creation modal for that date.
result: issue
reported: "fail, I still need to click the number"
severity: major

### 13. Popover-Positioned Task Modal
expected: When clicking a calendar cell, the task modal appears as a popover positioned near where you clicked, rather than centered on screen. It stays within viewport bounds.
result: pass

### 14. Keyboard Shortcuts
expected: With no input focused and no modal open: j = next month, k = previous month, t = today, m = month view, w = week view, c = open create task modal. These do nothing when typing in an input or when a modal is open.
result: issue
reported: "when you create a task modal with c it doesn't show the date. All modals should show the date with calendar dropdown to change it. Change keybindings: c = calendar view, l = list view, n = new task (defaults to today)"
severity: major

### 15. Settings Modal and Week Start Preference
expected: A settings button in the header (or pressing ?) opens a settings modal. It includes a start-of-week preference (Sunday/Monday). Changing it updates calendar day-name headers (Sun-Sat vs Mon-Sun) and persists across page reloads.
result: pass

## Summary

total: 15
passed: 9
issues: 6
pending: 0
skipped: 0

## Gaps

- truth: "Done task shows delay before disappearing, no pulsing amber ring"
  status: failed
  reason: "User reported: once task is marked as done, it disappears. I wanted a delay before it disappears. I don't want this pulsing amber ring."
  severity: major
  test: 3
  root_cause: "TaskListItem writes done status to DB immediately, Dexie live queries filter it out instantly. confirmReopen two-click logic with pulsing amber ring is unwanted."
  artifacts:
    - path: "src/components/list/TaskListItem.tsx"
      issue: "handleStatusClick writes to DB immediately with no departure delay; confirmReopen state and pulsing amber ring CSS are unwanted"
    - path: "src/db/hooks.ts"
      issue: "Live query filter t.status !== 'done' hides tasks instantly"
  missing:
    - "Remove confirmReopen state, two-click logic, and pulsing amber ring entirely"
    - "Add departure animation (strikethrough + fade ~1.5s) before writing done status to DB or use grace-period list"
  debug_session: ".planning/debug/done-tasks-disappear-immediately.md"

- truth: "Category combobox starts empty and shows selected category icon"
  status: failed
  reason: "User reported: the dropdown is not empty by default. And once the category is selected the icon doesn't show"
  severity: major
  test: 5
  root_cause: "TaskForm useEffect auto-selects first category (lines 49-53), preventing empty default. CategoryCombobox input has no icon rendering in closed state."
  artifacts:
    - path: "src/components/task/TaskForm.tsx"
      issue: "useEffect on lines 49-53 auto-selects first category when categoryId is 0"
    - path: "src/components/task/CategoryCombobox.tsx"
      issue: "Input element (lines 148-165) has no IconComponent rendering when dropdown is closed and category is selected"
  missing:
    - "Remove or conditionally skip auto-select useEffect for new tasks"
    - "Add IconComponent rendering before input when selectedCategory exists and dropdown is closed"
    - "Add left padding (pl-8) to input when icon is shown"
  debug_session: ".planning/debug/category-combobox-issues.md"

- truth: "Add button plus mark is centered in circle"
  status: failed
  reason: "User reported: the + mark is not centered in the circle"
  severity: cosmetic
  test: 8
  root_cause: "Raw + text glyph with text-lg line-height (28px) matches container h-7 (28px), preventing flexbox from optically centering the glyph due to font metrics."
  artifacts:
    - path: "src/components/list/DayGroup.tsx"
      issue: "Button on lines 45-51 uses text-lg without leading-none, + glyph sits below optical center"
  missing:
    - "Add leading-none class to the button to let flexbox truly center the glyph"
  debug_session: ""

- truth: "Inline create category field is larger, task title field slightly smaller"
  status: failed
  reason: "User reported: make category field slightly larger and task field slightly smaller"
  severity: cosmetic
  test: 10
  root_cause: "Title input uses flex-1 (greedy, takes all space); category select uses max-w-[140px] (hard cap, no growth). Proportions unbalanced."
  artifacts:
    - path: "src/components/task/TaskInlineCreate.tsx"
      issue: "Line 64: flex-1 on title is greedy; Line 69: max-w-[140px] on category is too restrictive"
  missing:
    - "Replace flex-1 with flex-[3] on title, remove max-w-[140px] and use flex-[1] min-w-[160px] on category"
  debug_session: ".planning/debug/inline-create-field-sizing.md"

- truth: "Full calendar cell is clickable to create task, not just the number"
  status: failed
  reason: "User reported: I still need to click the number"
  severity: major
  test: 12
  root_cause: "onClick={handleCellClick} is on an unsized inner div (line 45 of DayCell.tsx) that doesn't fill the grid cell. DroppableDay outer div has no click handler."
  artifacts:
    - path: "src/components/calendar/DayCell.tsx"
      issue: "Line 45: onClick on inner div that doesn't fill cell; Lines 37-43: DroppableDay has cursor-pointer but no click handler"
    - path: "src/components/dnd/DroppableDay.tsx"
      issue: "No onClick prop support to forward click handlers"
  missing:
    - "Make inner div fill cell with flex-1 and DroppableDay use flex flex-col, OR add onClick prop to DroppableDay"
  debug_session: ".planning/debug/daycell-click-area.md"

- truth: "Keyboard shortcuts: c=calendar, l=list, n=new task (today). Task modal always shows date with calendar dropdown."
  status: failed
  reason: "User reported: when you create a task modal with c it doesn't show the date. All modals should show the date with calendar dropdown to change it. Change keybindings: c = calendar view, l = list view, n = new task (defaults to today)"
  severity: major
  test: 14
  root_cause: "App.tsx keybindings map c to create task (should be calendar view), missing l (list) and n (new task). TaskForm has no date field at all - date is silently injected by TaskModal."
  artifacts:
    - path: "src/App.tsx"
      issue: "Lines 67-76: m/w/c keybindings need remapping; l and n need adding"
    - path: "src/components/task/TaskForm.tsx"
      issue: "No date field exists in the form - needs date input with calendar dropdown"
    - path: "src/components/task/TaskModal.tsx"
      issue: "Lines 41-43: silently injects date, needs to accept date from TaskForm instead"
  missing:
    - "Remap: c=calendar view, l=list view, n=new task; remove m and w bindings"
    - "Add date field with input type=date to TaskForm"
    - "Pass date to TaskForm, include in onSubmit payload"
    - "Update TaskModal to use date from form data"
  debug_session: ".planning/debug/keyboard-shortcuts-and-date-field.md"
