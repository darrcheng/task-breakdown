---
phase: 08-firebase-project-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .env.local
autonomous: false
requirements:
  - SETUP-03

must_haves:
  truths:
    - "Firebase project exists on Spark plan (not GCP trial credits)"
    - "Firestore database is provisioned in us-east1"
    - "Security rules are deployed and reject all reads/writes"
    - ".env.local has real Firebase config and app starts without Firebase errors"
  artifacts:
    - path: ".env.local"
      provides: "Real Firebase configuration values"
      contains: "VITE_FIREBASE_PROJECT_ID"
  key_links:
    - from: ".env.local"
      to: "src/firebase/config.ts"
      via: "Vite import.meta.env injection"
      pattern: "VITE_FIREBASE"
---

<objective>
Create the Firebase project in the console, fill in real credentials, deploy deny-all security rules, and verify the app starts cleanly with Firebase initialized.

Purpose: Complete the human-only steps (project creation, credential retrieval) and confirm the full setup works end-to-end.
Output: Live Firebase project on Spark plan, deployed security rules, working .env.local, app running without Firebase errors.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-firebase-project-setup/08-CONTEXT.md
@.planning/phases/08-firebase-project-setup/08-RESEARCH.md
@.planning/phases/08-firebase-project-setup/08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Firebase project and provide credentials</name>
  <files>.env.local</files>
  <action>User must create the Firebase project and Firestore database in the Firebase Console. Claude cannot do this — it requires Google account authentication and console interaction.

Steps for user:
1. Go to https://console.firebase.google.com/
2. Click "Add project" (or "Create a project")
3. Enter project name: `taskpad`
   - If the project ID `taskpad` is unavailable, try `taskpad-app` or `taskpad-xyz` — note the actual ID chosen
4. Disable Google Analytics (not needed)
5. Click "Create project"
6. VERIFY BILLING: Go to Project Settings > Usage and billing > confirm plan shows "Spark" (NOT Blaze with trial credits)
7. CREATE FIRESTORE: Go to Build > Firestore Database > Create database
   - Select location: `us-east1`
   - Start in **locked mode** (NOT test mode)
8. REGISTER WEB APP: Go to Project Settings > Your apps > click Web icon
   - App nickname: `taskpad-web`
   - Do NOT check "Firebase Hosting" checkbox (that is Phase 11)
   - Copy the 6 config values shown in the SDK snippet
9. Create `.env.local` in the project root by copying `.env.example` and replacing placeholders with real values from step 8
  </action>
  <verify>File `.env.local` exists with real VITE_FIREBASE_PROJECT_ID value (not placeholder)</verify>
  <done>Firebase project created on Spark plan, Firestore provisioned in us-east1, .env.local populated with real credentials</done>
</task>

<task type="auto">
  <name>Task 2: Deploy security rules and verify full setup</name>
  <files>.firebaserc</files>
  <action>
1. Authenticate with Firebase CLI:
   ```bash
   npx firebase login
   ```
   If auth fails, this becomes a checkpoint — user authenticates and Claude retries.

2. Verify the correct project is selected:
   ```bash
   npx firebase projects:list
   ```
   Confirm `taskpad` (or the user's chosen ID) appears. If the user chose a different project ID than `taskpad`, update `.firebaserc` to use the actual ID.

3. Deploy deny-all security rules:
   ```bash
   npx firebase deploy --only firestore:rules
   ```
   This deploys the `firestore.rules` file (deny all reads/writes).

4. Verify the app builds:
   ```bash
   npm run build
   ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Security rules deployed to Firebase (deny-all active). App builds without errors. Firebase project confirmed on Spark plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete setup</name>
  <files>n/a</files>
  <action>Present the verification checklist to the user. The app should already be built and the dev server can be started with `npm run dev`. All automated work is done — this is human visual/functional confirmation.</action>
  <verify>User confirms all items pass</verify>
  <done>User has verified: browser tab says "taskpad", no Firebase errors in console, Firestore rules deny all in Rules Playground, billing shows Spark plan</done>
</task>

</tasks>

<verification>
- Firebase project exists on Spark plan (verified in console)
- Firestore database provisioned in us-east1
- Security rules deployed and reject all reads/writes
- `.env.local` present with real Firebase config values
- `npm run build` passes
- `npm run dev` starts without Firebase errors in browser console
- Browser tab and header show "taskpad"
</verification>

<success_criteria>
- Firebase project on Spark plan (not trial credits)
- `npm run build` succeeds
- App starts and shows "taskpad" in title and header
- No Firebase errors in browser console
- Firestore rules reject unauthenticated reads/writes (verified in Rules Playground)
</success_criteria>

<output>
After completion, create `.planning/phases/08-firebase-project-setup/08-02-SUMMARY.md`
</output>
