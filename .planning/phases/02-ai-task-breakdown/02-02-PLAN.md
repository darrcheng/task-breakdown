---
phase: 02-ai-task-breakdown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/AIProviderSettings.tsx
  - src/components/settings/ProviderSetupModal.tsx
  - src/hooks/useAIProvider.ts
  - src/components/ui/SettingsModal.tsx
autonomous: true
requirements: [AI-07]

must_haves:
  truths:
    - "User can select between Claude and Gemini providers in settings"
    - "User can enter and save API key for selected provider"
    - "API key is encrypted before storage"
    - "User can change provider without losing tasks"
    - "Connection test confirms API key works before saving"
  artifacts:
    - path: "src/components/settings/AIProviderSettings.tsx"
      provides: "Provider selection UI and API key entry form"
      contains: "AIProviderSettings"
    - path: "src/components/settings/ProviderSetupModal.tsx"
      provides: "First-use provider configuration modal"
      contains: "ProviderSetupModal"
    - path: "src/hooks/useAIProvider.ts"
      provides: "Hook for loading/saving AI provider config and creating provider instances"
      exports: ["useAIProvider"]
  key_links:
    - from: "src/hooks/useAIProvider.ts"
      to: "src/ai/key-storage.ts"
      via: "saveApiKey/loadApiKey calls"
      pattern: "saveApiKey|loadApiKey"
    - from: "src/hooks/useAIProvider.ts"
      to: "src/ai/provider-factory.ts"
      via: "createProvider call"
      pattern: "createProvider"
    - from: "src/components/ui/SettingsModal.tsx"
      to: "src/components/settings/AIProviderSettings.tsx"
      via: "renders AIProviderSettings section"
      pattern: "AIProviderSettings"
---

<objective>
Build the AI provider settings UI and first-use configuration flow so users can set up their preferred AI provider before using task breakdown.

Purpose: Users need a way to configure their AI provider and API key. The first-use flow (triggered from "Break it down" button in Plan 03) guides initial setup, while the settings page allows changing provider later.
Output: Settings section for AI provider, first-use setup modal, and useAIProvider hook.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-task-breakdown/02-RESEARCH.md
@.planning/phases/02-ai-task-breakdown/02-CONTEXT.md
@src/components/ui/SettingsModal.tsx
@src/hooks/useSettings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAIProvider hook and provider settings component</name>
  <files>src/hooks/useAIProvider.ts, src/components/settings/AIProviderSettings.tsx</files>
  <action>
**src/hooks/useAIProvider.ts:**
Create a hook that manages AI provider configuration:
- State: `{ provider: AIProviderName | null, hasKey: boolean, isLoading: boolean, error: string | null }`
- On mount: Read provider preference from localStorage (`taskbreaker-ai-provider`), check if API key exists (via `loadApiKey` — just checks non-null, does not decrypt unnecessarily)
- `configureProvider(provider: AIProviderName, apiKey: string)`: Save provider name to localStorage, encrypt and save API key via `saveApiKey()`, test connection via `createProvider().testConnection()`, set error if test fails
- `getProvider(): Promise<AIProvider | null>`: Load API key, create provider instance, return it (or null if not configured)
- `isConfigured`: boolean derived from provider !== null && hasKey === true
- `clearProvider()`: Remove provider from localStorage, delete API key

**src/components/settings/AIProviderSettings.tsx:**
Create a settings section component (not a modal — embedded in SettingsModal):
- Shows current provider selection (radio buttons or toggle: Claude / Gemini)
- Shows masked API key status: "API key saved (****xyz)" or "No API key"
- "Change API Key" button reveals password input + "Save" / "Cancel" buttons
- "Test Connection" button that calls testConnection() and shows success/failure
- Provider info text: Claude = "Direct browser connection (no proxy needed)", Gemini = "May require CORS proxy for browser usage"
- Uses Tailwind classes consistent with existing SettingsModal styling (slate color scheme, rounded-lg, text-sm)
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Verify component renders correctly when imported into SettingsModal (next task).
  </verify>
  <done>
useAIProvider hook loads/saves provider config with encrypted key storage. AIProviderSettings component shows provider selection, key status, and test connection button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create first-use setup modal and integrate into SettingsModal</name>
  <files>src/components/settings/ProviderSetupModal.tsx, src/components/ui/SettingsModal.tsx</files>
  <action>
**src/components/settings/ProviderSetupModal.tsx:**
Create a standalone modal for first-time AI provider setup (triggered when user first taps "Break it down" without a configured provider):
- Step 1: "Choose your AI provider" — two cards: Claude (recommended, direct browser connection) and Gemini (may need proxy)
- Step 2: "Enter your API key" — password input field, link to where to get the key (console.anthropic.com or aistudio.google.com depending on selection), "Test & Save" button
- Step 3: "Connected!" — success confirmation with "Start Breaking Down Tasks" button
- Loading/error states for connection test
- Close/cancel button
- Uses the same overlay pattern as TaskModal (fixed inset-0, z-50, bg-black/50 backdrop, centered white panel)
- Props: `isOpen: boolean`, `onClose: () => void`, `onConfigured: () => void` (called after successful setup)

**src/components/ui/SettingsModal.tsx:**
Add a new "AI Provider" section below the existing "Start of week" section:
- Import and render `<AIProviderSettings />` component
- Add a divider (`border-t border-slate-200`) between sections
- Pass `settings` and `onUpdateSettings` if needed, or let AIProviderSettings use its own useAIProvider hook
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Open settings modal in dev mode, verify "AI Provider" section appears below "Start of week". Open ProviderSetupModal independently to verify the 3-step flow renders correctly.
  </verify>
  <done>
ProviderSetupModal guides first-time users through provider selection and API key entry with connection test. SettingsModal includes AI Provider section for changing provider/key later. Both components use consistent Tailwind styling.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds
- Settings modal shows AI Provider section with provider toggle and key management
- ProviderSetupModal renders 3-step setup flow
- Connection test works (with valid API key)
- API key is encrypted in localStorage (inspect storage — should see ciphertext, not plaintext)
</verification>

<success_criteria>
Users can configure AI provider in settings. First-use modal guides initial setup. API keys encrypted before storage. Connection test validates keys. Provider can be changed without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-task-breakdown/02-02-SUMMARY.md`
</output>
