---
phase: 03-adhd-optimized-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/task/TaskForm.tsx
  - src/components/task/TaskModal.tsx
  - src/components/task/TaskCard.tsx
  - src/components/list/TaskListItem.tsx
  - src/App.tsx
  - src/db/hooks.ts
autonomous: true
requirements:
  - ADHD-01

must_haves:
  truths:
    - "User can select low/medium/high energy level when creating or editing a task"
    - "Energy level displays as a colored chip on task cards in calendar view"
    - "Energy level displays on task list items in list view"
    - "User can filter tasks by energy level from the app header"
    - "Clearing the energy filter shows all tasks again"
    - "Energy filter applies to both calendar and list views"
  artifacts:
    - path: "src/components/task/TaskForm.tsx"
      provides: "Energy level chip selector (3 toggle buttons)"
      contains: "EnergyLevel"
    - path: "src/App.tsx"
      provides: "Energy filter state and filter chips in header"
      contains: "energyFilter"
    - path: "src/db/hooks.ts"
      provides: "Energy level filtering in useTasksByDate and useTasksByDateRange"
      contains: "energyLevel"
  key_links:
    - from: "src/App.tsx"
      to: "src/db/hooks.ts"
      via: "energyFilter prop threaded through views to query hooks"
      pattern: "energyFilter"
    - from: "src/components/task/TaskForm.tsx"
      to: "src/types/index.ts"
      via: "EnergyLevel type for energy chip state"
      pattern: "EnergyLevel"
---

<objective>
Add energy level tagging to tasks and global energy filtering so users can tag tasks by effort and filter by current capacity.

Purpose: ADHD users often struggle with task initiation because they can't match tasks to their current energy level. This feature lets them tag and filter by low/medium/high energy, reducing decision paralysis.

Output: Energy chips in TaskForm, energy badge on TaskCard and TaskListItem, energy filter in App header, filtered query hooks.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adhd-optimized-ux/03-CONTEXT.md
@.planning/phases/03-adhd-optimized-ux/03-RESEARCH.md
@.planning/phases/03-adhd-optimized-ux/03-01-SUMMARY.md
@src/types/index.ts
@src/components/task/TaskForm.tsx
@src/components/task/TaskCard.tsx
@src/components/list/TaskListItem.tsx
@src/App.tsx
@src/db/hooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add energy level selector to TaskForm and display on TaskCard and TaskListItem</name>
  <files>src/components/task/TaskForm.tsx, src/components/task/TaskCard.tsx, src/components/list/TaskListItem.tsx</files>
  <action>
Energy tagging UI design is Claude's discretion. Use 3-chip horizontal toggle selector with battery/zap icons per research recommendations.

In `src/components/task/TaskForm.tsx`:
- Import `Battery, BatteryMedium, Zap` from lucide-react (or `BatteryLow` if available — check lucide-react exports; if not available, use `Battery` for low).
- Import `EnergyLevel` from types.
- Add `energyLevel` to form state, initialized from `initialData?.energyLevel ?? null`.
- Add energy chip selector UI below the status pills and above the category combobox. Render 3 toggle buttons:
  ```
  Low (Battery icon, sky colors: text-sky-600 bg-sky-50 border-sky-300 when selected)
  Medium (BatteryMedium icon, amber colors: text-amber-600 bg-amber-50 border-amber-300 when selected)
  High (Zap icon, emerald colors: text-emerald-600 bg-emerald-50 border-emerald-300 when selected)
  ```
  Unselected state: `text-slate-500 bg-white border-slate-300 hover:border-slate-400`.
  Click to select, click again to deselect (set null). Each chip: `flex items-center gap-1 px-2 py-1 rounded-full border text-xs font-medium transition-colors`.
- Add a label "Energy" (text-sm font-medium text-slate-700) above the chips, matching the existing form field label pattern.
- Include `energyLevel` in the data object passed to `onSubmit`.

In `src/components/task/TaskCard.tsx`:
- Import `Battery, BatteryMedium, Zap` from lucide-react and `EnergyLevel` from types.
- After the task title, add a small energy badge if `task.energyLevel` is set:
  ```typescript
  const ENERGY_DISPLAY: Record<EnergyLevel, { icon: LucideIcon; color: string; label: string }> = {
    low: { icon: Battery, color: 'text-sky-500', label: 'Low' },
    medium: { icon: BatteryMedium, color: 'text-amber-500', label: 'Med' },
    high: { icon: Zap, color: 'text-emerald-500', label: 'High' },
  };
  ```
  Render as: `<span className="flex items-center gap-0.5 text-[10px]"><Icon className="w-3 h-3" />{label}</span>` — compact to fit in the calendar card.

In `src/components/list/TaskListItem.tsx`:
- Import the same `ENERGY_DISPLAY` pattern (or define inline).
- Add energy badge between the task title and the ParentBadge, similar to TaskCard but slightly larger (text-xs).
- Only show if `task.energyLevel` is truthy.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Create a task, select energy level, save — energy chip should appear on task card</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>TaskForm has 3-chip energy selector. TaskCard shows energy badge. TaskListItem shows energy badge. Energy level saves to DB and persists.</done>
</task>

<task type="auto">
  <name>Task 2: Add energy filter to App header and thread through views</name>
  <files>src/App.tsx, src/db/hooks.ts</files>
  <action>
Energy filter placement is Claude's discretion. Place filter chips in the App header, right of ViewToggle, matching the existing `showCompleted` toggle pattern.

In `src/App.tsx`:
- Import `EnergyLevel` from types.
- Import `Battery, BatteryMedium, Zap` from lucide-react.
- Add state: `const [energyFilter, setEnergyFilter] = useState<EnergyLevel | null>(null);`
- In the header, after the ViewToggle and before the showCompleted button, add 3 small filter chips:
  ```
  Low (Battery, sky highlight when active)
  Medium (BatteryMedium, amber highlight when active)
  High (Zap, emerald highlight when active)
  ```
  Click toggles filter on/off (same chip pattern as TaskForm but smaller — `px-1.5 py-0.5 text-xs`). Active chip uses the energy color; inactive is `text-slate-400 border-slate-200 hover:border-slate-300`.

  Wrap in a div with a subtle separator (border-l border-slate-200 pl-3 ml-1) to visually group them.

- Thread `energyFilter` as a prop through to the calendar and list views:
  - `CalendarGrid`: add `energyFilter` prop, pass down to `DayCell`
  - `WeekView`: add `energyFilter` prop, pass down to day cells
  - `ListView`: add `energyFilter` prop, pass down to `DayGroup`
  - Each component threads it to the `useTasksByDate` or `useTasksByDateRange` call.

In `src/db/hooks.ts`:
- Update `useTasksByDate` signature to accept optional `energyFilter: EnergyLevel | null`:
  ```typescript
  export function useTasksByDate(date: string, showCompleted: boolean, energyFilter?: EnergyLevel | null)
  ```
  In the query filter, add: `&& (!energyFilter || t.energyLevel === energyFilter)`.
  Add `energyFilter` to the deps array: `[date, showCompleted, energyFilter]`.

- Update `useTasksByDateRange` identically — add optional `energyFilter` param, filter, and dep.

Note: `energyFilter` is `EnergyLevel | null`. When null, no filtering occurs (all tasks shown). When set, only tasks matching that energy level are shown. Tasks with no energy level set (`null`/`undefined`) will be hidden when a filter is active — this is intentional (encourages tagging).

Import `EnergyLevel` from types in hooks.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Click energy filter chip in header — calendar and list views should only show tasks with matching energy level</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>App header has 3 energy filter chips. Clicking one filters both calendar and list views to that energy level. Clicking again clears filter. useTasksByDate and useTasksByDateRange accept and apply energyFilter.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- TaskForm shows energy chip selector with 3 options
- TaskCard displays energy badge when set
- TaskListItem displays energy badge when set
- App header has energy filter chips
- Filtering by energy level works in both calendar and list views
- Clearing filter shows all tasks
</verification>

<success_criteria>
- TypeScript compiles clean
- Build succeeds
- Energy level can be set on new and existing tasks
- Energy badges visible on task cards and list items
- Energy filter in header filters both views
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-02-SUMMARY.md`
</output>
