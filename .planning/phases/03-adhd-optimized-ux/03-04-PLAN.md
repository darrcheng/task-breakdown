---
phase: 03-adhd-optimized-ux
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/calendar/OverdueBanner.tsx
  - src/components/overdue/OverdueQuickPicker.tsx
  - src/components/overdue/SomedayView.tsx
  - src/App.tsx
  - src/components/ui/ViewToggle.tsx
autonomous: true
requirements:
  - TASK-08
  - ADHD-04

must_haves:
  truths:
    - "User sees a closeable banner at top of calendar view when overdue tasks exist"
    - "Banner uses warm, casual tone without guilt language"
    - "Dismissed banner does not reappear until the next day"
    - "Tapping banner opens quick picker listing all overdue tasks"
    - "Each overdue task has reschedule (date picker), archive to Someday, and mark done actions"
    - "Bulk 'Move all to today' and 'Send all to Someday' actions work with one tap"
    - "Someday list is accessible via a third ViewMode toggle"
    - "Tasks sent to Someday do not appear in calendar or list views"
    - "Tasks can be moved out of Someday back to a date"
  artifacts:
    - path: "src/components/calendar/OverdueBanner.tsx"
      provides: "Dismissable banner with warm tone and review button"
      contains: "OverdueBanner"
    - path: "src/components/overdue/OverdueQuickPicker.tsx"
      provides: "Modal with per-task actions and bulk actions"
      contains: "OverdueQuickPicker"
    - path: "src/components/overdue/SomedayView.tsx"
      provides: "View for Someday-archived tasks with restore actions"
      contains: "SomedayView"
    - path: "src/components/ui/ViewToggle.tsx"
      provides: "Three-way toggle: calendar, list, someday"
      contains: "someday"
    - path: "src/App.tsx"
      provides: "OverdueBanner in calendar view, OverdueQuickPicker modal state, SomedayView rendering"
      contains: "OverdueBanner"
  key_links:
    - from: "src/components/calendar/OverdueBanner.tsx"
      to: "src/db/hooks.ts"
      via: "useOverdueTasks hook for overdue task count"
      pattern: "useOverdueTasks"
    - from: "src/components/overdue/OverdueQuickPicker.tsx"
      to: "src/db/database.ts"
      via: "db.tasks.update for reschedule/someday/done actions"
      pattern: "db\\.tasks\\.update"
    - from: "src/App.tsx"
      to: "src/components/overdue/SomedayView.tsx"
      via: "viewMode === 'someday' conditional rendering"
      pattern: "viewMode.*someday"
---

<objective>
Build the overdue task nudge system (banner + quick picker with per-task and bulk actions) and the Someday list for tasks the user wants to remember but isn't committing to a date.

Purpose: ADHD users experience guilt and avoidance when they see overdue tasks. A warm, non-judgmental banner with easy "fresh start" options removes the emotional burden and helps them re-engage.

Output: OverdueBanner component, OverdueQuickPicker modal, SomedayView, updated ViewToggle with 3 modes.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adhd-optimized-ux/03-CONTEXT.md
@.planning/phases/03-adhd-optimized-ux/03-RESEARCH.md
@.planning/phases/03-adhd-optimized-ux/03-01-SUMMARY.md
@.planning/phases/03-adhd-optimized-ux/03-02-SUMMARY.md
@src/App.tsx
@src/components/ui/ViewToggle.tsx
@src/db/hooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OverdueBanner, OverdueQuickPicker, and SomedayView components</name>
  <files>src/components/calendar/OverdueBanner.tsx, src/components/overdue/OverdueQuickPicker.tsx, src/components/overdue/SomedayView.tsx</files>
  <action>
Create `src/components/calendar/OverdueBanner.tsx`:
- Props: `{ taskCount: number; onOpenPicker: () => void }`.
- Use localStorage key `'taskbreaker-overdue-dismissed'` for daily dismissal.
- Capture `today` via `useMemo(() => formatDateKey(new Date()), [])` for session stability (avoids midnight flicker).
- Initialize `dismissed` from `useState(() => localStorage.getItem(DISMISS_KEY) === today)`.
- If `dismissed || taskCount === 0`, return null.
- Render a warm amber banner:
  ```
  div: mx-4 mt-2 px-4 py-2.5 bg-amber-50 border border-amber-200 rounded-lg flex items-center justify-between text-sm text-amber-800
  ```
- Message text (warm, casual, no guilt):
  - 1 task: `"You've got 1 task from earlier — want to move it?"`
  - N tasks: `"You've got ${taskCount} tasks from earlier this week — want to move them?"`
- Right side: "Review" link button (font-medium underline) calls `onOpenPicker`, and X close button (lucide X icon) calls `handleDismiss`.
- `handleDismiss`: sets localStorage to today's date string and sets dismissed to true.

Create `src/components/overdue/OverdueQuickPicker.tsx`:
- Props: `{ isOpen: boolean; onClose: () => void; tasks: Task[] }`.
- Render as a div-based overlay modal (same pattern as TaskModal: fixed inset-0, bg-black/30 backdrop, centered white panel).
- Panel: `max-w-md w-full mx-4 bg-white rounded-xl shadow-2xl max-h-[80vh] flex flex-col`.
- Header: "Overdue Tasks" title + X close button.
- Scrollable task list in the middle:
  - Each task shows: title, date it was due (text-xs text-slate-400), and 3 action buttons:
    1. **Reschedule**: Calendar icon button. On click, show inline date picker (use the existing DatePicker component from `'../task/DatePicker'`). When date selected, call `db.tasks.update(task.id, { date: newDate, updatedAt: new Date() })`.
    2. **Someday**: Archive icon button. On click, call `db.tasks.update(task.id, { isSomeday: true, updatedAt: new Date() })`.
    3. **Done**: CheckCircle icon button. On click, call `db.tasks.update(task.id, { status: 'done', updatedAt: new Date() })`.
  - Use `Calendar, Archive, CheckCircle2` from lucide-react.
  - Each action should remove the task from the list reactively (since the parent passes tasks from `useOverdueTasks` which is reactive).
- Bulk actions footer (sticky bottom):
  - "Move all to today" button: `bg-blue-600 text-white rounded-lg px-4 py-2 text-sm font-medium`. On click:
    ```typescript
    await Promise.all(tasks.map(t => db.tasks.update(t.id!, { date: formatDateKey(new Date()), updatedAt: new Date() })));
    onClose();
    ```
  - "Send all to Someday" button: `text-slate-600 border border-slate-300 rounded-lg px-4 py-2 text-sm font-medium`. On click:
    ```typescript
    await Promise.all(tasks.map(t => db.tasks.update(t.id!, { isSomeday: true, updatedAt: new Date() })));
    onClose();
    ```
- If `tasks.length === 0`, show "All caught up!" message and auto-close after 1 second.
- Click outside (backdrop) closes the picker.

Create `src/components/overdue/SomedayView.tsx`:
- Import `useSomedayTasks` from db/hooks.
- Import `useCategoryMap` from db/hooks if needed (or accept as prop).
- Props: `{ categoryMap: Map<number, Category> | undefined }`.
- Render the Someday list:
  - Header: "Someday" title with `Archive` icon, subtitle "Tasks you want to remember but aren't committing to a date."
  - List of someday tasks, each showing:
    - Task title
    - Category icon
    - "Rescue" button (CalendarPlus or CalendarDays icon): shows inline DatePicker. On date selection, sets `isSomeday: false` and updates `date` to selected date.
    - Delete (Trash2 icon) with click-to-confirm pattern (matching existing project pattern): first click shows red background, second click within 3s deletes.
  - Empty state: "Nothing here yet. Tasks you send to Someday will appear here." with a friendly Archive icon.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Check that all 3 new component files exist and compile</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>OverdueBanner renders warm dismissable banner. OverdueQuickPicker shows all overdue tasks with per-task and bulk actions. SomedayView lists someday tasks with rescue and delete actions.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate OverdueBanner, OverdueQuickPicker, and SomedayView into App and update ViewToggle</name>
  <files>src/App.tsx, src/components/ui/ViewToggle.tsx</files>
  <action>
In `src/components/ui/ViewToggle.tsx`:
- The `ViewMode` type (imported from types) now includes `'someday'`.
- Add a third button to the toggle group for Someday view.
- Use the `Archive` icon from lucide-react for the Someday button.
- Follow the same button pattern as existing Calendar/List toggles.
- Label: "Someday" (hidden on small screens with `hidden sm:inline`).

In `src/App.tsx`:
- Import `OverdueBanner` from `'./components/calendar/OverdueBanner'`.
- Import `OverdueQuickPicker` from `'./components/overdue/OverdueQuickPicker'`.
- Import `SomedayView` from `'./components/overdue/SomedayView'`.
- Import `useOverdueTasks` from `'./db/hooks'`.
- Add state: `const [isQuickPickerOpen, setIsQuickPickerOpen] = useState(false);`
- Call `const overdueTasks = useOverdueTasks();` at the top of the component.
- Add keyboard shortcut guard: add `isQuickPickerOpen` to the modal-open guard in the keydown handler.
- Add `'s'` keyboard shortcut to switch to someday view (add to switch statement):
  ```
  case 's':
    e.preventDefault();
    setViewMode('someday');
    break;
  ```
- In the JSX, after MonthNavigation and before EmptyState, render the OverdueBanner (only in calendar view):
  ```tsx
  {viewMode === 'calendar' && overdueTasks && overdueTasks.length > 0 && (
    <OverdueBanner
      taskCount={overdueTasks.length}
      onOpenPicker={() => setIsQuickPickerOpen(true)}
    />
  )}
  ```
- In the main content area, add SomedayView as a third view option:
  ```tsx
  {viewMode === 'someday' ? (
    <SomedayView categoryMap={categoryMap} />
  ) : viewMode === 'calendar' ? (
    // ... existing calendar rendering
  ) : (
    // ... existing list rendering
  )}
  ```
  Note: Wrap in DndProvider only for calendar/list views. SomedayView does not need drag-and-drop.
  Actually, move SomedayView outside the DndProvider. Restructure:
  ```tsx
  {viewMode === 'someday' ? (
    <main className="flex-1 flex flex-col overflow-hidden">
      <SomedayView categoryMap={categoryMap} />
    </main>
  ) : (
    <DndProvider categoryMap={categoryMap}>
      <main className="flex-1 flex flex-col overflow-hidden">
        {/* existing calendar/list rendering */}
      </main>
    </DndProvider>
  )}
  ```
- Render the OverdueQuickPicker modal at the bottom (alongside TaskModal, CategoryManager, SettingsModal):
  ```tsx
  <OverdueQuickPicker
    isOpen={isQuickPickerOpen}
    onClose={() => setIsQuickPickerOpen(false)}
    tasks={overdueTasks ?? []}
  />
  ```
- Hide MonthNavigation when viewMode is 'someday' (it only applies to calendar):
  ```tsx
  {viewMode === 'calendar' && (
    <MonthNavigation ... />
  )}
  ```
  This is already the case in existing code — verify it still works.
- Update the SettingsModal SHORTCUTS array (if it's in SettingsModal) to include the new `s` shortcut for Someday view.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Open app with overdue tasks — banner should appear. Click Review to open quick picker. Test Move all to today. Switch to Someday view via toggle.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>OverdueBanner appears in calendar view when overdue tasks exist. Quick picker opens with per-task and bulk actions. ViewToggle has 3 modes. SomedayView accessible. s keyboard shortcut works. All modals guarded in keyboard handler.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- OverdueBanner appears when tasks are past due
- Banner dismissal persists for the day via localStorage
- Quick picker shows all overdue tasks with 3 actions each
- "Move all to today" moves all tasks to today's date
- "Send all to Someday" archives all to someday
- ViewToggle has Calendar / List / Someday options
- SomedayView shows archived tasks with rescue and delete
- Tasks in Someday do not appear in calendar or list views
</verification>

<success_criteria>
- TypeScript compiles clean
- Build succeeds
- Overdue banner is warm and guilt-free
- Quick picker enables easy bulk rescheduling
- Someday list is accessible and functional
- All navigation and keyboard shortcuts work
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-04-SUMMARY.md`
</output>
