---
phase: 03-adhd-optimized-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/ai/providers/types.ts
  - src/ai/providers/anthropic.ts
  - src/ai/providers/gemini.ts
  - src/ai/prompts.ts
  - src/utils/estimateCalibration.ts
  - src/hooks/useTimeEstimate.ts
  - src/components/task/TaskModal.tsx
  - src/components/task/TaskCard.tsx
autonomous: true
requirements:
  - ADHD-02

must_haves:
  truths:
    - "User sees a time estimate badge on task cards after AI generates one"
    - "Time estimate appears in task modal with detail view"
    - "User can tap the estimate to manually override it"
    - "Overrides persist and are used instead of AI estimates"
    - "AI estimates generate automatically in the background after task save"
    - "Time estimates are consistent with past estimates for similar tasks (calibration)"
  artifacts:
    - path: "src/ai/providers/types.ts"
      provides: "estimateTime method on AIProvider interface"
      contains: "estimateTime"
    - path: "src/ai/providers/anthropic.ts"
      provides: "Anthropic implementation of estimateTime"
      contains: "estimateTime"
    - path: "src/ai/providers/gemini.ts"
      provides: "Gemini implementation of estimateTime"
      contains: "estimateTime"
    - path: "src/ai/prompts.ts"
      provides: "buildTimeEstimatePrompt function"
      contains: "buildTimeEstimatePrompt"
    - path: "src/utils/estimateCalibration.ts"
      provides: "localStorage calibration map for estimate corrections"
      contains: "getCalibrationHint"
    - path: "src/hooks/useTimeEstimate.ts"
      provides: "Background AI estimation hook with fire-and-forget pattern"
      contains: "useTimeEstimate"
  key_links:
    - from: "src/hooks/useTimeEstimate.ts"
      to: "src/ai/providers/types.ts"
      via: "AIProvider.estimateTime call"
      pattern: "provider\\.estimateTime"
    - from: "src/hooks/useTimeEstimate.ts"
      to: "src/db/database.ts"
      via: "db.tasks.update to write estimate back"
      pattern: "db\\.tasks\\.update.*timeEstimate"
    - from: "src/components/task/TaskModal.tsx"
      to: "src/hooks/useTimeEstimate.ts"
      via: "triggerEstimate called after task save"
      pattern: "triggerEstimate"
---

<objective>
Add AI-powered time estimation with background generation, badge display, manual override, and calibration learning from user corrections.

Purpose: ADHD users struggle with time blindness. Seeing realistic time estimates helps them plan their day and reduces the anxiety of "I don't know how long this will take."

Output: estimateTime on AIProvider, background estimation hook, estimate badge on TaskCard, estimate display + override in TaskModal, calibration utility.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adhd-optimized-ux/03-CONTEXT.md
@.planning/phases/03-adhd-optimized-ux/03-RESEARCH.md
@.planning/phases/03-adhd-optimized-ux/03-01-SUMMARY.md
@src/ai/providers/types.ts
@src/ai/providers/anthropic.ts
@src/ai/providers/gemini.ts
@src/ai/prompts.ts
@src/hooks/useAIProvider.ts
@src/components/task/TaskModal.tsx
@src/components/task/TaskCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add estimateTime to AIProvider interface, implement in both providers, create prompt and calibration utils</name>
  <files>src/ai/providers/types.ts, src/ai/providers/anthropic.ts, src/ai/providers/gemini.ts, src/ai/prompts.ts, src/utils/estimateCalibration.ts</files>
  <action>
In `src/ai/providers/types.ts`:
- Add `estimateTime` method to the `AIProvider` interface:
  ```typescript
  estimateTime(
    taskTitle: string,
    taskDescription: string,
    calibrationHint: string,
  ): Promise<number | null>;
  ```
  This is a single-shot (non-streaming) call that returns estimated minutes or null on failure.

In `src/ai/prompts.ts`:
- Add `buildTimeEstimatePrompt` function:
  ```typescript
  export function buildTimeEstimatePrompt(
    taskTitle: string,
    taskDescription: string,
    calibrationHint: string,
  ): string {
    return `Estimate how long this task will take in minutes.

  **Task:** ${taskTitle}
  ${taskDescription ? `**Description:** ${taskDescription}` : ''}
  ${calibrationHint ? `**Calibration note:** ${calibrationHint}` : ''}

  **Rules:**
  - Return ONLY a JSON object: {"minutes": <number>}
  - Round to nearest: 5, 10, 15, 20, 30, 45, 60, 90, 120, 180, 240
  - If the task is vague or could vary greatly, pick the median realistic estimate
  - Do not explain your answer

  Example: {"minutes": 30}`;
  }
  ```

In `src/ai/providers/anthropic.ts`:
- Add `estimateTime` method to the `AnthropicProvider` class.
- Use a non-streaming call: `this.client.messages.create({ model: this.model ?? 'claude-sonnet-4-20250514', max_tokens: 50, messages: [{ role: 'user', content: prompt }] })`.
  Wait — check the existing code for what model is used. The existing `generateSubtasks` likely uses a specific model. Follow the same pattern. If no model field exists, use `'claude-sonnet-4-20250514'` for cost efficiency on a simple task.
- Parse the response text content, extract JSON `{"minutes": N}`, return the number. Use try/catch — return null on parse failure.
- Use `buildTimeEstimatePrompt` to construct the prompt.

In `src/ai/providers/gemini.ts`:
- Add `estimateTime` method to the `GeminiProvider` class.
- Use a non-streaming call via the existing `@google/genai` SDK: `this.client.models.generateContent(...)` or equivalent.
- Follow the same model selection pattern as `generateSubtasks` (use `this.model` if it exists on the class).
- Parse JSON response, return minutes or null.
- Use `buildTimeEstimatePrompt` to construct the prompt.

In `src/utils/estimateCalibration.ts` (NEW file):
- localStorage key: `'taskbreaker-estimate-calibration'`
- Store calibration entries as: `{ categoryId: number, aiEstimate: number, userOverride: number }[]`
- Export functions:
  - `recordCalibration(categoryId: number, aiEstimate: number, userOverride: number): void` — appends entry to localStorage array (keep last 50 entries to avoid unbounded growth).
  - `getCalibrationHint(categoryId: number): string` — reads entries for the given categoryId, computes average correction ratio (userOverride / aiEstimate), returns hint string like `"Your past tasks in this category averaged ~45 minutes (AI estimated ~30 min). Adjust accordingly."` If no entries for this category, return empty string.
  - `formatEstimate(minutes: number): string` — `minutes < 60 ? \`${minutes}m\` : \`${Math.round(minutes / 60 * 10) / 10}h\`` (e.g., "15m", "1.5h", "2h").
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Both provider classes implement estimateTime, prompts.ts has buildTimeEstimatePrompt, estimateCalibration.ts exports all 3 functions</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>AIProvider interface has estimateTime. Both AnthropicProvider and GeminiProvider implement it with non-streaming calls. buildTimeEstimatePrompt generates constrained prompt. estimateCalibration.ts stores/retrieves correction history and formats estimates.</done>
</task>

<task type="auto">
  <name>Task 2: Create useTimeEstimate hook, add estimate badge to TaskCard, add estimate display and override to TaskModal</name>
  <files>src/hooks/useTimeEstimate.ts, src/components/task/TaskCard.tsx, src/components/task/TaskModal.tsx</files>
  <action>
In `src/hooks/useTimeEstimate.ts` (NEW file):
- Import `useCallback, useRef` from react.
- Import `useAIProvider` hook (check exact export — likely `useAIProvider` from `'./useAIProvider'`).
- Import `db` from database, `getCalibrationHint` from estimateCalibration, `buildTimeEstimatePrompt` from prompts.
- Export `useTimeEstimate` hook that returns `{ triggerEstimate }`.
- `triggerEstimate(taskId: number, title: string, description: string, categoryId: number)`:
  - Uses an AbortController ref to cancel in-flight requests when a new one starts for the same task.
  - Gets the AI provider via `getProvider()` from useAIProvider. If no provider configured, return silently.
  - Gets calibration hint via `getCalibrationHint(categoryId)`.
  - Calls `provider.estimateTime(title, description, calibrationHint)`.
  - If result is a valid number > 0, writes to DB: `db.tasks.update(taskId, { timeEstimate: result, updatedAt: new Date() })`.
  - Wrap entire body in try/catch — log errors to console but never throw (fire-and-forget).
- Use `useRef` for the AbortController to support cancellation across re-renders.
- IMPORTANT: Do NOT await this in the calling component's submit handler. The caller should call `triggerEstimate(...)` without await so the UI proceeds immediately.

In `src/components/task/TaskCard.tsx`:
- Import `formatEstimate` from `'../../utils/estimateCalibration'`.
- Compute `effectiveEstimate = task.timeEstimateOverride ?? task.timeEstimate`.
- After the task title (and energy badge if present), render the estimate badge if effectiveEstimate is truthy:
  ```tsx
  {effectiveEstimate && (
    <span className="text-[10px] text-slate-400 flex-shrink-0 whitespace-nowrap">
      ~{formatEstimate(effectiveEstimate)}
    </span>
  )}
  ```

In `src/components/task/TaskModal.tsx`:
- Import `useTimeEstimate` hook.
- Import `formatEstimate, recordCalibration` from estimateCalibration.
- In the modal, call `const { triggerEstimate } = useTimeEstimate();`.
- After task save (in the handleSubmit or equivalent), call `triggerEstimate(taskId, title, description, categoryId)` WITHOUT await. Only trigger if the task doesn't already have a timeEstimate (skip on edits where title hasn't changed significantly — simplest: only trigger on create, not edit. Or trigger on create and on title change during edit).
  Decision: trigger on create always, and on edit only if title changed. Compare `data.title !== initialData?.title`.
- Add a time estimate display section in the modal body (below description, above subtasks if present):
  ```
  [Clock icon] Estimated: ~30m     [Edit pencil icon to override]
  ```
  Show `effectiveEstimate = task.timeEstimateOverride ?? task.timeEstimate`. If neither exists, show nothing (estimate pending or AI not configured).
- For override: when user clicks the pencil/edit icon, show a small inline input (number type, step=5, min=5, max=480). On blur or Enter, save the override:
  ```typescript
  await db.tasks.update(taskId, { timeEstimateOverride: overrideValue, updatedAt: new Date() });
  recordCalibration(task.categoryId, task.timeEstimate ?? 0, overrideValue);
  ```
  This stores the correction for future calibration.
- Import `Clock, Pencil` from lucide-react for the estimate display.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <manual>Create a task with AI configured — after a few seconds, estimate badge should appear on the task card. Open modal, see estimate detail, tap to override.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>useTimeEstimate hook fires background AI estimation. TaskCard shows ~Xm badge. TaskModal shows estimate detail with override capability. Overrides recorded for calibration. No UI blocking during estimation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds
- AIProvider.estimateTime exists on interface and both providers
- buildTimeEstimatePrompt constrains output to JSON {"minutes": N}
- estimateCalibration.ts manages localStorage calibration history
- useTimeEstimate hook triggers background estimation
- TaskCard shows estimate badge
- TaskModal shows estimate detail with override
- Override writes back and records calibration
</verification>

<success_criteria>
- TypeScript compiles clean
- Build succeeds
- Time estimates generate automatically after task creation (when AI configured)
- Estimates display on task cards and in modal
- User can override estimates
- Calibration data persists for future estimates
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-03-SUMMARY.md`
</output>
