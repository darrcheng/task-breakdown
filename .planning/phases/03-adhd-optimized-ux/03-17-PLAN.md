---
phase: 03-adhd-optimized-ux
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/hooks.ts
autonomous: true
requirements: [TASK-07]
gap_closure: true

must_haves:
  truths:
    - "AI-generated subtasks appear as independent task rows on the calendar view"
    - "AI-generated subtasks appear as independent task rows in the list view"
    - "Overdue hook still filters out subtasks (subtasks inherit parent's date, not independently overdue)"
    - "Someday hook still filters out subtasks (subtasks are not independently someday-able)"
  artifacts:
    - path: "src/db/hooks.ts"
      provides: "Subtasks visible in calendar and list views"
      contains: "removed !t.parentId from useTasksByDate and useTasksByDateRange"
  key_links:
    - from: "useTasksByDate filter predicates"
      to: "Calendar day cells and list view DayGroup"
      via: "Dexie liveQuery"
      pattern: "useTasksByDate"
---

<objective>
Restore subtask visibility on calendar and list views by removing the `!t.parentId` filter from `useTasksByDate` and `useTasksByDateRange`. This filter was incorrectly added in commit 6cf4e30 — Claude misinterpreted user feedback about subtasks not showing in list view as a "data leak bug" and removed them from BOTH views. The user explicitly wants subtasks as independent task rows in both views.

Purpose: User confirmed this was an intentional feature. Subtasks should appear alongside parent tasks on the calendar and list.
Output: Subtasks appear as task rows in calendar and list views.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/debug/subtask-calendar-feature-removed.md
@src/db/hooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove parentId filter from date query hooks</name>
  <files>src/db/hooks.ts</files>
  <action>
Remove `!t.parentId` from the filter predicates in `useTasksByDate` and `useTasksByDateRange`. There are 4 occurrences total (2 per function: one for showCompleted=false, one for showCompleted=true).

**useTasksByDate (lines 16 and 18):**

Line 16 (showCompleted=false):
- BEFORE: `return query.filter((t) => t.status !== 'done' && !t.isSomeday && !t.parentId && (!energyFilter || t.energyLevel === energyFilter)).toArray();`
- AFTER: `return query.filter((t) => t.status !== 'done' && !t.isSomeday && (!energyFilter || t.energyLevel === energyFilter)).toArray();`

Line 18 (showCompleted=true):
- BEFORE: `return query.filter((t) => !t.isSomeday && !t.parentId && (!energyFilter || t.energyLevel === energyFilter)).toArray();`
- AFTER: `return query.filter((t) => !t.isSomeday && (!energyFilter || t.energyLevel === energyFilter)).toArray();`

**useTasksByDateRange (lines 41 and 43):**

Line 41 (showCompleted=false):
- BEFORE: `return query.filter((t) => t.status !== 'done' && !t.isSomeday && !t.parentId && (!energyFilter || t.energyLevel === energyFilter)).toArray();`
- AFTER: `return query.filter((t) => t.status !== 'done' && !t.isSomeday && (!energyFilter || t.energyLevel === energyFilter)).toArray();`

Line 43 (showCompleted=true):
- BEFORE: `return query.filter((t) => !t.isSomeday && !t.parentId && (!energyFilter || t.energyLevel === energyFilter)).toArray();`
- AFTER: `return query.filter((t) => !t.isSomeday && (!energyFilter || t.energyLevel === energyFilter)).toArray();`

**DO NOT change:**
- `useOverdueTasks` (line 117) — keep `!t.parentId` here. Subtasks inherit parent's date; they shouldn't independently appear as overdue.
- `useSomedayTasks` (line 128) — keep `!t.parentId` here. Subtasks aren't independently someday-able.
- `useSubtasks` and `useSubtaskCount` — these are separate queries by parentId, unaffected.

This is a 4-place removal of `&& !t.parentId` from filter predicates.
  </action>
  <verify>
    <automated>cd C:/Users/JenLab-User/task-breakdown && npx tsc --noEmit && npm run build</automated>
    <manual>Create a task, run AI breakdown to generate subtasks. Check calendar view — subtasks should appear as individual task cards on the same day. Check list view — subtasks should appear as individual rows. Verify overdue banner does NOT include subtasks of non-overdue parents.</manual>
  </verify>
  <done>Subtasks appear as independent task rows on calendar and list views. Overdue and someday queries still filter out subtasks.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds
- `useTasksByDate` and `useTasksByDateRange` do NOT contain `!t.parentId`
- `useOverdueTasks` and `useSomedayTasks` still contain `!t.parentId`
</verification>

<success_criteria>
AI-generated subtasks appear as independent rows/cards in both calendar and list views, restoring the feature that was incorrectly removed in commit 6cf4e30.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-17-SUMMARY.md`
</output>
