---
phase: 03-adhd-optimized-ux
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/task/SubtaskList.tsx
autonomous: true
requirements: [ADHD-03]
gap_closure: true

must_haves:
  truths:
    - "Completing a subtask shows emerald ring glow + green background, then settles into done styling (slate-400 line-through) without ever becoming invisible"
    - "Subtask remains visible at all times during and after the celebration animation"
    - "Re-clicking a celebrating subtask cancels animation and reverts to todo"
  artifacts:
    - path: "src/components/task/SubtaskList.tsx"
      provides: "Celebration-only animation for SubtaskRow (no opacity-0)"
      contains: "bg-emerald-50"
  key_links:
    - from: "SubtaskRow departingPhase state"
      to: "CSS classes on container div"
      via: "clsx conditional classes"
      pattern: "departingPhase.*fade.*bg-emerald"
---

<objective>
Fix SubtaskRow celebration animation: replace departure animation (opacity-0 fade-out) with celebration-only animation (ring glow + green background, then settle into done styling). Subtasks never leave the tree, so opacity-0 makes no sense and causes a visible disappear-then-reappear glitch.

Purpose: UAT test 2 failed because subtasks become invisible during the fade phase, then reappear when departingPhase resets. User wants: "Just do the fade to green and cross out."
Output: SubtaskRow with celebration animation that keeps the subtask visible at all times.
</objective>

<execution_context>
@C:/Users/JenLab-User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/JenLab-User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-adhd-optimized-ux/03-11-SUMMARY.md
@.planning/debug/subtask-fade-reappear.md
@src/components/task/SubtaskList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SubtaskRow departure animation with celebration-only animation</name>
  <files>src/components/task/SubtaskList.tsx</files>
  <action>
In SubtaskRow (inside SubtaskList.tsx), replace the departure animation that uses opacity-0 with a celebration-only animation. The key insight: subtasks NEVER leave the tree (useSubtasks has no status filter), so opacity-0 is destructive — it hides the subtask and then it reappears.

**Changes to the container div's clsx (around lines 207-214):**

Replace the fade phase class:
- BEFORE: `departingPhase === 'fade' && 'ring-2 ring-emerald-400 ring-offset-1 opacity-0 transition-all duration-[1500ms]'`
- AFTER: `departingPhase === 'fade' && 'ring-2 ring-emerald-400 ring-offset-1 bg-emerald-50 transition-all duration-[1500ms]'`

The ONLY change is removing `opacity-0` and replacing it with `bg-emerald-50`. This keeps the subtask visible during the entire animation while showing a green celebration background.

The ring phase class stays the same (no opacity-0 there — it just shows the ring).
The settling phase class stays the same (transition-all duration-300 for smooth ring fade-out).
The `!departing && 'transition-colors'` stays the same.
The isStartHere violet ring stays the same.

**No changes needed to:**
- The departure state machine timing (ring -> double-rAF -> fade -> 1500ms -> settling -> 400ms -> null -> DB write)
- The handleStatusClick logic
- The title text styling (already correct: green line-through during ring/fade, slate-400 line-through when done)
- The re-click cancellation logic
- The cleanup effects

This is a ONE-LINE change: swap `opacity-0` for `bg-emerald-50` on the fade phase class string.
  </action>
  <verify>
    <automated>cd C:/Users/JenLab-User/task-breakdown && npx tsc --noEmit && npm run build</automated>
    <manual>Open TaskModal with subtasks. Complete a subtask. Should see: emerald ring glow appears, background turns green (bg-emerald-50), text turns green with line-through. After ~1.5s, ring fades out smoothly (settling phase). Subtask settles into done styling (slate-400 line-through). Subtask is NEVER invisible.</manual>
  </verify>
  <done>Subtask celebration animation shows ring glow + green background, then transitions to done styling. No opacity-0, no disappear-reappear. Subtask remains visible throughout.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds
- SubtaskList.tsx fade phase uses `bg-emerald-50` instead of `opacity-0`
- No `opacity-0` appears in any SubtaskRow animation phase
</verification>

<success_criteria>
Completing a subtask in the subtask tree shows celebration (ring glow + green bg + green line-through), then smoothly settles into done styling (slate-400 line-through). Subtask is visible at all times. Re-click cancels and reverts to todo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-adhd-optimized-ux/03-15-SUMMARY.md`
</output>
